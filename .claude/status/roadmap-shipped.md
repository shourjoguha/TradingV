# Roadmap — SHIPPED archive

> Phases 0-6 of the forecasting-tool → decision-support-tool transition. **All ✅ live as of 2026-04-27.** Kept for reference: each phase's spec, exit criteria, and post-ship retrospective.
>
> **Active roadmap** lives at [roadmap.md](roadmap.md). For the principles that drove the sequencing, see [principles.md](../guides/principles.md). For per-decision detail (e.g. "why Telegram over email"), see [decisions/](../decisions).

The shift: forecasting tool (Kronos predictions in, charts out, human decides) → decision-support tool (predictions → tested signals → tracked actions → measured outcomes). Each phase gated on the prior delivering real signal.

Reference brainstorm: chat session 2026-04-27.

## Retrospective notes (1 line per phase)

- **Per-chart time controls on Sectors page** (2026-05-18): operator follow-up after the bump chart shipped — wanted per-chart time-period customization (e.g. "see yearly or monthly, go back 5 years instead of just 12 weeks") across all Plotly charts on `/macro/sectors`. Audited the surfaces: cycle wheel + phase-confirmation badges already use the page-level `1Y/3Y/5Y/10Y/Max` chip (no change needed); rotation bump + correlation matrix were hardcoded (`weeks=12, daysPerWeek=5` and `window=90` respectively). PredictionsByTarget candlestick excluded — already has its own date/interval pickers. Ratios page also excluded — ChartBuilder uses the page-level chip. **What landed**: (1) New `components/charts/ChartTimeControl.tsx` — small generic `ToggleGroup`-backed primitive parameterized over preset-id type, designed for per-chart override of a single time axis. Sits in the chart's `CardHeader` next to the `InfoBubble`. (2) `RotationFootprintStrip.tsx` extended w/ `CADENCE_PRESETS = [12w (weekly), 26w (weekly), 1y · mo, 3y · mo, 5y · mo]` — each maps to `(periods, daysPerPeriod)` passed to `weeklyRankMatrix`. Default unchanged (12w weekly). InfoBubble copy updated to mention the cadence selector + data-ceiling note ("pick 5y on page-level chip to unlock 5y monthly"). (3) `CorrelationHeatmap.tsx` extended w/ `WINDOW_PRESETS = [30d, 90d, 180d, 1y, 3y, 5y]` in trading days. The selected window cascades to BOTH the matrix and the click-drill rolling-pair line (operator picks once, both views update). Header subtitle now reads "{N}-day window · average off-diagonal correlation: {x}"; drill-in subtitle reads "Rolling {N}-day Pearson correlation: A vs B". Both math helpers (`correlationMatrix`, `rollingPairCorrelation`) already supported arbitrary window arg + clamp gracefully via `slice(-window-1)` — no math changes needed. **Decisions logged**: NO URL state for these selectors (sectors page is exploration-mode; bookmark/share belongs on /macro/ratios ChartBuilder); NO per-chart override on cycle wheel or phase badges (RS-indexed math contract is anchored on `RS_LOOKBACK_BASE = 252` for operator-known reasons; changing the base shifts meaning); NO per-chart override on Macro Overview or Ratios (page-level chip already handles those — duplicating fragments UX); curated 5 cadence presets over independent `(cadence × lookback)` toggles (independent toggles double UI surface w/o decision payoff). **Verification**: TS clean; Playwright `.playwright-mcp/timecontrol/rotation-default-12w.png` shows 12w weekly default w/ selector at top-right, `rotation-5y-monthly.png` shows 60-snapshot 5y monthly view w/ many crossings + lines spanning Jul 2021 → Jan 2026, `correlation-default-90d.png` shows 90d default + window selector, `correlation-1y.png` shows 252-day window recomputed (header reads "252-day window · average off-diagonal correlation: -.04 dispersed", all cell values changed). Selector is `ToggleGroup` w/ matching `text-xs font-mono` styling — visually mirrors the page-level time chips so the operator pattern-matches the interaction. **Operator-facing**: rotation bump now answers "how has leadership evolved across this business cycle?" (5y monthly) AND "is the recent shift sticky?" (12w weekly) from the same card; correlation matrix can show secular cohesion (5y) vs regime cohesion (90d) vs near-term shock cohesion (30d). Both via 1-click toggle. **Files modified/created**: new — `components/charts/ChartTimeControl.tsx`; modified — `components/macro/RotationFootprintStrip.tsx` (cadence state + selector wire-up + InfoBubble copy), `components/macro/CorrelationHeatmap.tsx` (window state + selector + cascade to drill-in + subtitle updates), `.claude/frontend/charts.md` (per-chart time controls section), `.claude/modules/macro.md` (per-viz blurbs updated). **Pattern available for future**: any Plotly chart that needs a per-chart time override drops in `<ChartTimeControl value onChange presets ariaLabel />` w/ its own preset constant. Generic preset-id type means each chart owns its axis vocabulary.

- **Chart enrichment + ChartBuilder primitive (Phases A-E)** (2026-05-18): operator follow-up after the charts-plotly migration shipped — wanted (1) rotation footprint redesigned to show arrows/movement, (2) correlation matrix gaining a time axis, (3) Macro Ratios enriched w/ multi-ratio overlay + chart-type toggle + stackable panes, (4) a "custom chart builder helper module" the rest of the app could plug into. Council deliberation (visual designer / quant / skeptic / motion / UX strategist / operator advocate) converged: bump chart > sankey for rank-over-time (Sankey is for non-conserved flow magnitudes; rank tracking is conserved); click-cell drill-in beats persistent strip for correlation time view (low cognitive load); ChartBuilder extracted as a real primitive per operator's "future services plug into it" mandate; URL-persist for bookmark/share-able views. **What landed**: (1) `components/charts/plotly/BumpChart.tsx` — reusable rank-over-time scatter+lines primitive, smoothed via `line.shape: 'spline'`, dual Y-axis labels (left = first-snapshot landing, right = last-snapshot landing per operator request), no labels repeated per bump. (2) `RotationFootprintStrip.tsx` rewritten — domain wrapper shapes weekly footprint into `BumpSeries[]`, all 9 sectors as matte identity-color lines crossing on rank flips. Filename preserved for caller stability. (3) `lib/sector-strength.ts` extended w/ two new pure helpers: `weeklyRankMatrix(seriesBySymbol, weeks, daysPerWeek)` returns full rank per sector per week (not just top-K) for the bump chart, and `rollingPairCorrelation(a, b, window=90)` for the heatmap drill-in. (4) `components/charts/plotly/Heatmap.tsx` extended w/ `onCellClick(row, col, value)` callback prop; `PlotlyChart` wrapper gained `onClick` passthrough for Plotly mouse events. (5) `CorrelationHeatmap.tsx` rewritten w/ click-drill: `selectedPair` state on cell click → `rollingPairCorrelation` series → `<LineChart>` renders below the matrix w/ "Rolling 90-day Pearson correlation: A vs B" header + clear-X button. Diagonal clicks ignored. (6) **Tier 3 ChartBuilder primitive** in `components/charts/builder/` — `ChartBuilder.tsx` (N stacked panes + "Add pane" button), `Pane.tsx` (chart-type toggle line/area/log + series chips + remove buttons), `SeriesPicker.tsx` (dropdown over `AvailableSeries[]` registry, excludes already-added IDs), `use-resolved-series.ts` (parallel TanStack `useQueries` resolving N specs against the same cache keys `useMacroRatio`/`useMacroSeries` use — cache-aware, no double-fetch), `url-state.ts` (compact text encoding `panes=ratio:XLK/SPY,ratio:XLE/SPY\|type:line;ratio:HG/GC\|type:area` ≤ ~2KB practical), `types.ts` (SeriesSpec discriminated union `ratio` vs `series`, PaneSpec, ChartType, AvailableSeries, ResolvedSeries). (7) `pages/Macro.tsx` `RatiosTab` rewritten as a `<ChartBuilder>` consumer: `availableSeries` derived from existing `ALL_ROWS`, default pane = first ratio (preserves legacy view), URL syncs via `useSearchParams` (elides default state to keep URLs clean). **Bundle impact** (gzip, vs Phase 6 baseline): main bundle unchanged at **205 KB** (chart infra stays lazy); PlotlyChart chunk unchanged at **403 KB** (no new trace registrations); Macro chunk **12.8 KB → 17.2 KB** (+4.5 KB for ChartBuilder + Pane + SeriesPicker + URL state). Hot path zero cost. **Verification**: TS clean across all 5 phases; Playwright `.playwright-mcp/enrich/A-bump.png` shows bump chart w/ smoothed lines + sector identity colors + crossings; `B-heatmap-drill.png` shows clicking XLK/XLE cell renders rolling 90d Pearson line 2021→2026 below the matrix; `D-builder-default.png` shows the new Line/Area/Log Y toggle + series chips + "+ Add series" + "+ Add pane" controls; `D-builder-multi-pane.png` shows 2 panes (line w/ Gold + Copper/Gold overlay; area w/ RSP/SPY) driven from URL `?panes=ratio:GC=F/SPY:Gold,ratio:HG=F/GC=F:Copper/Gold|type:line;ratio:RSP/SPY|type:area`. Bookmark/share works. **Decisions logged** (skeptic-driven): did NOT register `sankey` Plotly trace (operator picked bump over Sankey — saves ~80 KB on PlotlyChart chunk); did NOT add `spreads` / `OHLC` / `heatmap` to ChartBuilder V1 (ratio + series only — extend when a 2nd consumer surface demands them); did NOT switch SeriesPicker to a typeahead Combobox (native `<select>`-style menu is fine for 12-15 ratios; promote when registry grows past ~30); did NOT enrich PredictionsByTarget yet (operator listed it but no specific asks; today's chart works — defer until they iterate). **Operator-facing**: rotation footprint now reads as 9 colored lines crossing — leadership swaps visible at a glance, no per-week color decoding required; correlation cell-click drill-in lets the operator answer "is XLE actually rotating *away* from XLK or just temporarily?" in 2 clicks; Macro Ratios can now stack 5+ panes w/ overlay ratios and a saved URL — power-user view alongside the simple default. **What this completes**: charts-plotly migration's end state for the year — Tier 1 SVG primitives + Tier 2 Plotly individual viz + Tier 3 ChartBuilder composable. Future viz needs (e.g. PredictionsByTarget multi-horizon overlay control, hypothesis-evolution stacked panes) plug into ChartBuilder w/o new architecture. **Files modified/created**: new — `components/charts/plotly/BumpChart.tsx`, `components/charts/builder/{ChartBuilder.tsx,Pane.tsx,SeriesPicker.tsx,use-resolved-series.ts,url-state.ts,types.ts}`; modified — `components/charts/plotly/{Heatmap.tsx,PlotlyChart.tsx}` (click event passthrough), `components/charts/index.ts` (Tier 3 barrel), `components/macro/{RotationFootprintStrip.tsx,CorrelationHeatmap.tsx}` (use new primitives), `pages/Macro.tsx` (RatiosTab now `<ChartBuilder>`), `lib/sector-strength.ts` (+`weeklyRankMatrix`, +`rollingPairCorrelation`). Migration plan: `.claude/plans/charts-enrichment.md`.

- **Chart infra refactor + Plotly migration — Phases 5-6 (full reorg complete)** (2026-05-17): completes the chart-infra arc started in Phases 0-4 (see entry directly below). Operator green-lit shipping the deferred phases in one go. **Phase 5a (CorrelationHeatmap → Plotly)**: uncomment heatmap in `plotly-bundle.ts`; new `components/charts/plotly/Heatmap.tsx` (~90 LOC) takes `{ rows, cols, z, colorscale, zmin, zmax }`, defaults to the `CORRELATION_GRADIENT` palette stop (-1 red → 0 grey → +1 green) from the chart theme. `CorrelationHeatmap.tsx` rewritten as a data-only domain wrapper — shapes the `matrix` record into the 2D z-grid Plotly expects, computes the avg off-diagonal cohesion summary, delegates render. Bonus from Plotly: native colorbar legend on the right + cell hover w/ row/col/value template, neither of which the bespoke HTML-table version had. Removed: bespoke `corrColor()` + `corrFg()` helpers + 6 rgba literals (now sourced from palette). **Phase 5b (CyclePhaseWheel → Plotly)**: uncomment scatterpolar + barpolar in `plotly-bundle.ts`; new `components/charts/plotly/PolarRadial.tsx` (~150 LOC) composes barpolar (quadrant tints) + scatterpolar (sector dots w/ markers + labels) + polar annotations (outer quadrant labels + center "you are here" callout). Takes `{ quadrants, dots, centerText, radius, height }`. Includes a `ccwAngle()` helper that converts the operator's clockwise-from-12 angle convention to Plotly's counter-clockwise-from-3 polar convention so callers can keep their mental model. `CyclePhaseWheel.tsx` rewritten as a data wrapper: shapes `PolarQuadrant[]` from `PHASES` metadata + active-phase detection, shapes `PolarDot[]` from `SECTOR_PHASE` + `sectorRs` (size = `dotRadius(rs)` mapping [80,120] → [6,16] px). Removed: hand-rolled `donutSectorPath()` + `polar()` math + `dotPositionsForPhase()` SVG layout helpers + ~150 LOC of inline `<svg>` JSX. Visual delta vs bespoke SVG: quadrant tints are softer (barpolar transparency 0.06 inactive / 0.22 active vs SVG fill-opacity 0.06 / 0.18); sector labels position slightly above dots (scatterpolar `textposition: 'top center'`); center text + outer quadrant labels rendered via Plotly annotations (more flexible — operator can add more annotation layers in the future without touching SVG geometry). Acceptable trade-off in exchange for: native pan/hover, future RRG-style continuous trails via scatterpolar `mode: 'lines+markers'` (just pass `dots` w/ historical snapshots as additional traces). **Phase 6a (folder reorg of SVG primitives)**: moved `components/macro/Sparkline.tsx` → `components/charts/svg/Sparkline.tsx` (5 callsite import path updates); moved `components/common/DriftBar.tsx` → `components/charts/svg/DriftBar.tsx` (3 callsite updates: `RxStrip.tsx`, `RecRow.tsx`, `RxFinance.tsx`); extracted `MiniCandleCompare` inline component from `PredictionsByHorizon.tsx` (the audit-missed Tier-1 chart) → `components/charts/svg/MiniCandleCompare.tsx` (~110 LOC standalone, `Ohlc` type also exported). PredictionsByHorizon now imports `MiniCandleCompare` instead of defining it inline — 526→457 LOC. **Phase 6b (Sparkline divide-by-zero fix)**: confirmed real bug — old line 74 `((last-first)/first)*100` returned `Infinity` / `NaN` whenever the leading value was zero (Sectors-ladder z-score sparklines often start at ~0). Fix: guard `Math.abs(first) < 1e-9` → fall back to absolute delta in series units + tag `usesAbsolute=true` so the suffix renders as a unit-less number (`+0.42`) instead of `%`. Callers don't change. Non-z-scored macro-ratio sparklines render identically because their `first` value is never zero. **Phase 6c (delete old shims + barrel update)**: deleted `components/macro/Sparkline.tsx`, `components/macro/RatioChart.tsx` (the Phase 2 re-export shim), `components/common/DriftBar.tsx`. Updated `components/macro/RegimePanel.tsx`, `SectorStrip.tsx`, `pages/Macro.tsx` to import `LineChart` directly from `components/charts/plotly/LineChart` (aliased `as RatioChart` to keep the name familiar where the operator sees it in dev tools). Updated `components/charts/index.ts` barrel — now exports all Tier 1 + Tier 2 components + types. **Bundle impact** (production build, gzip, vs Phase 4 baseline): main bundle unchanged at **205 KB** (chart code stays lazy — Sparkline + DriftBar + MiniCandleCompare all SVG primitives w/ zero chart-lib cost); PlotlyChart lazy chunk grew from 372 KB → **403 KB** (+31 KB for the heatmap + scatterpolar + barpolar trace types added in `plotly-bundle.ts`); Macro lazy chunk grew from 12.4 KB → 12.8 KB (+0.4 KB for the new Heatmap/PolarRadial imports). Net hot-path cost: **zero**. Net per-route cost on /macro: +31 KB gzip — paid once + browser-cached, gets you a true polar chart + a native heatmap. **Verification**: TS clean across the rewrite; Playwright `.playwright-mcp/after-p5/03-wheel.png` + `06-correlation.png` confirm Plotly renders w/ quadrant tints + sector dots + center callout (wheel) and 9×9 colored grid + native colorbar legend + value labels (heatmap); `.playwright-mcp/after-p6/08-predictions-horizon.png` confirms MiniCandleCompare extraction works (page renders w/ 0 errors, empty-state message comes from no-predictions-for-date, not a chart bug); `.playwright-mcp/after-p6/09-today.png` confirms Sparkline + DriftBar via new import paths; `.playwright-mcp/after-p6/10-motion-recs.png` confirms DriftBar import path swap. **Files modified/created in Phases 5-6**: new — `components/charts/plotly/{Heatmap.tsx,PolarRadial.tsx}`, `components/charts/svg/{Sparkline.tsx,DriftBar.tsx,MiniCandleCompare.tsx}`; modified — `components/charts/plotly-bundle.ts` (added 3 trace registrations), `components/charts/index.ts` (full Tier 1 + Tier 2 barrel), `components/macro/{CorrelationHeatmap.tsx,CyclePhaseWheel.tsx,RegimePanel.tsx,SectorStrip.tsx,SectorLadderCard.tsx}` (import paths + delegate render), `components/today/RxStrip.tsx`, `components/rx/RecRow.tsx`, `pages/RxFinance.tsx`, `pages/Macro.tsx`, `pages/PredictionsByHorizon.tsx` (~115 LOC of inline `MiniCandleCompare` deleted); deleted — `components/macro/{Sparkline.tsx,RatioChart.tsx}`, `components/common/DriftBar.tsx`. **What this completes**: two-tier chart infra now fully realized — `components/charts/svg/*` (Tier 1, zero-cost SVG primitives for cell/widget/popover use) + `components/charts/plotly/*` (Tier 2, Plotly-backed for expanded views) + `components/charts/theme/*` (single source of truth for chart colors). Operator's "modular and centralized" + "Plotly only when needed" mandate both satisfied. **Decisions logged**: did NOT register `bar` trace (no callsite — would have cost ~50 KB on the lazy chunk for nothing); did NOT migrate `RegimeConditionalBadges` or `RotationFootprintStrip` to Plotly (the former isn't actually a chart — text/icon matrix; the latter is a CSS grid that's a poor fit for Plotly heatmap because of the categorical y-axis + per-cell color identity); did NOT delete the dead `_MiniCandleCompare_DEAD_KEEP_FOR_REFERENCE` comment marker (it was a refactoring-intermediate state that got removed in the same edit before commit). **Operator-facing**: chart-related work for new viz types now starts in one place — `components/charts/index.ts` shows what's available, `theme/palette.ts` shows what colors to use, `plotly-bundle.ts` shows what to uncomment for a new trace type.

- **Chart infra refactor + Plotly migration (Phases 0-4)** (2026-05-17): operator asked for both (a) chart-modularity reorg and (b) move to Plotly for complex viz patterns lightweight-charts can't serve (RRG-style polar w/ tails, true heatmaps, parallel coordinates). Council picked a **two-tier infra**: Tier 1 = inline SVG primitives in `components/charts/svg/` (Sparkline, DriftBar, MiniCandleCompare — cheap, used across table cells / card widgets / popovers); Tier 2 = Plotly-backed in `components/charts/plotly/` (LineChart, CandlestickChart, future Heatmap/PolarRadial — feature-rich, lazy-loaded per route). **What shipped (Phases 0-4)**: (1) `components/charts/theme/{palette,layout,tokens}.ts` — single source of truth for chart hex literals; replaces `RatioChart` `PALETTE`, `PredictionsByTarget` `PREDICTION_COLORS`, and inline `#5FAFA8/#E07A6F/#94A3B8` in `Sparkline.tsx`. `SECTOR_IDENTITY_HEX` dedup still pending Phase 6. (2) `components/charts/plotly-bundle.ts` — custom Plotly build via `plotly.js/lib/core` + `Plotly.register([scatter, candlestick])`. Heatmap, scatterpolar, bar deferred to Phase 5 (uncomment when callsites land). (3) `components/charts/plotly/PlotlyChart.tsx` — base wrapper w/ neumorphic theme injected, ResizeObserver-driven width, loading/empty states, suppressed modebar. (4) `components/charts/plotly/LineChart.tsx` — replaces lightweight-charts `RatioChart` (re-exported from old path for caller-stability; `Macro.tsx`, `SectorStrip.tsx`, `DetailSidecar.tsx` unchanged). (5) `components/charts/plotly/CandlestickChart.tsx` — replaces inline `createChart` block in `PredictionsByTarget.tsx` (~150 LOC ripped out, replaced w/ ~30 LOC of `useMemo` shaping data into Plotly traces). (6) `vite.config.ts` gained `define: { global: 'globalThis' }` to fix Plotly's `has-hover` dep referencing Node's `global` in the browser. (7) `App.tsx` now lazy-loads Predictions (same pattern as Macro) so Plotly stays off the Today / Decide hot path. (8) `npm uninstall lightweight-charts`. **Bundle impact** (production build, gzip): main bundle 651 KB → **205 KB** (**−446 KB on hot path** because Plotly is lazy); new lazy chunks PlotlyChart=372 KB (loaded on /macro + /predictions), Predictions=36 KB, Macro=12 KB. Plotly chunk ended up bigger than the 700 KB raw target (1.07 MB raw / 372 KB gzip) because `plotly.js/lib/core` includes ~800 KB of d3 + utils that can't be tree-shaken further without a build-time hack; gzip number is what matters for transport. **What was kept as-is**: `Sparkline.tsx`, `DriftBar.tsx`, `MiniCandleCompare` (Tier 1 SVG primitives — used inline in >3 instances per page; Plotly per-cell would balloon DOM + memory). `CorrelationHeatmap`, `CyclePhaseWheel`, `RotationFootprintStrip`, `RegimeConditionalBadges` (Phase 5 — operator-gated). **What was missed by the prior audit** (`chart-modularity-handover.md`): (a) `PredictionsByHorizon.tsx` (526 LOC) embeds `MiniCandleCompare` as inline SVG — audit's grep didn't catch it because the candles use Tailwind class strokes not hex literals; (b) `PredictionsByTarget` was 516 LOC not 350 — win #1 effort estimate (1h) was low; actual ~2h. **Stress-test findings carried over**: Sparkline divide-by-zero on z-scored series (line 74, `((last-first)/first)*100` when `first===0`) — confirmed bug; fix slated for Phase 6. **Visual-parity check**: Playwright before/after screenshots captured for `/macro`, `/macro/ratios`, `/macro/sectors` (×4 dropdown viz), `/predictions/target`, `/predictions/horizon`, `/today` in `.playwright-mcp/baselines/` + `.playwright-mcp/after-p2/` + `.playwright-mcp/after-p3/`. Visual parity: line color + axis range identical; cosmetic differences = Y-ticks moved right→left (Plotly default), TradingView watermark gone (lightweight-charts attribution), no last-value pill on right axis. **Files modified/created** (10): `components/charts/{index.ts,plotly-bundle.ts,theme/{palette.ts,layout.ts,tokens.ts},plotly/{PlotlyChart.tsx,LineChart.tsx,CandlestickChart.tsx}}` new; `components/macro/RatioChart.tsx` (rewritten as re-export, 116→4 LOC); `pages/PredictionsByTarget.tsx` (516→457 LOC); `vite.config.ts`, `App.tsx`, `package.json`. **Deferred next**: Phase 5 (CorrelationHeatmap + CyclePhaseWheel → Plotly Heatmap + scatterpolar — would gain free hover/zoom + true RRG-style tails), Phase 6 (folder reorg moving Sparkline/DriftBar/MiniCandle into `components/charts/svg/` + fix divide-by-zero). Migration plan + open risks live in `.claude/plans/charts-plotly-migration.md`.

- **Macro Sectors round 3 — 4-view dropdown selector** (2026-05-17): operator's third iteration on the Sectors sub-tab. After shipping the Cycle Phase Wheel as the primary viz, they asked to ALSO build the 3 deferred surfaces (rotation footprint, regime-conditional badges, 9×9 correlation heatmap) and expose all 4 via a dropdown — flip-through-perspectives pattern, one viz mounted at a time, grid + always-on chart stay constant beneath. **What landed**: (1) `lib/sector-strength.ts` extended w/ two new pure helpers — `weeklyRotationFootprint(seriesBySymbol, weeks=12, topK=3, daysPerWeek=5)` returns `Array<{weekEnding, topK}>` w/ a snapshot every ~5 daily points walking back from the tail (each snapshot reruns `rsRankBySymbol` on a sliced series — O(weeks × symbols × N) total), and `correlationMatrix(closesBySymbol, window=90)` computes pairwise Pearson correlation on daily log-returns over the trailing window (diagonal pinned to 1). (2) New `components/macro/RotationFootprintStrip.tsx` — horizontal scrollable strip of 12 weekly columns, each column showing top-3 sectors as identity-bar chips stacked rank-1-to-3, date label below each column. Operator scans left→right: stable stack = persistent leadership, frequent color changes = active rotation. Live read: leadership stabilized into XLK/XLE/XLI for the last 6 weeks after early-year flip-flopping. (3) New `components/macro/CorrelationHeatmap.tsx` — 9×9 matrix on daily log-returns over 90d. Cell color = correlation magnitude (green = positive, red = negative, grey = uncorrelated, max alpha 0.55). Diagonal = "—". Header shows average off-diagonal correlation + categorical label (tight cohesion / moderate / dispersed). Live read: avg +0.26 → "dispersed" — sectors trading independently; XLE shows negative correlations w/ XLK/XLY (energy moving counter to cyclicals); XLK·XLY 0.67, XLF·XLY 0.59, XLB·XLI 0.70 = cyclical-corner cluster. (4) New `components/macro/RegimeConditionalBadges.tsx` — per-sector verdict matrix: each sector tagged Confirming (✓ in-phase + leading) / Out-of-phase leader (⚑ off-phase + leading — SIGNAL) / Failing canonical (✗ in-phase + lagging — DIVERGENCE) / Quiet (·). Sorted by signal priority (out-of-phase → confirming → failing → quiet). Live read: XLE ⚑ +10.9% out-of-phase leader (Energy favored Late, currently Mid — investigate); XLK ✓ +19.7% confirming Mid; XLI ✗ −3.1% failing canonical (Mid-favored Industrials not leading — breadth concern). (5) `pages/Macro.tsx` `SectorsTab` rewritten w/ `<select>` dropdown above the viz area, `view` state cycles through 4 options, ternary mount of the active viz; `SectorStrip` (compact grid + always-on chart) stays mounted constantly beneath so context survives view flips. **Verification**: TS clean; Playwright `.audit/view{1,2,3,4}-*.png` show each view rendering w/ real data, dropdown swap works, bottom grid + chart unchanged across flips. All 4 viz reuse the same `useMacroRatio` / `useMacroSeries` cache (TanStack 5-min stale time) so flipping between views is instant after first load — no double-fetch. **Math contracts** (defensible, no proprietary frameworks): rotation = repeated `rsRankBySymbol` on sliced series; correlation = Pearson on log-returns; phase-confirmation = `verdictFor(favored, current, rsIndexed)` w/ `>100` leading threshold. **Operator-actionable narrative emerging from the views**: late-cycle commodity strength (XLE out-of-phase ⚑) + Mid-cycle Industrials weakness (XLI failing ✗) + cyclical-corner correlation cluster (XLK/XLY/XLI/XLF) + Tech-led leadership stabilization (rotation footprint last 6 weeks) → composite read of "Mid cycle persisting w/ early Late hints in commodities, Tech sole confirmed leader."
- **Macro Sectors round 2 — compact grid + Cycle Phase Wheel + always-on chart** (2026-05-17): operator iterated on the RS Leadership Ladder: liked the relative numbers + always-on chart at the bottom; asked to (a) drop the Yield-curve regime-panel pin (redundant — already on Overview), (b) condense the full-width ladder rows back to a 3-col grid pattern, (c) build the Cycle Phase Wheel that was deferred in the original plan. **What landed**: (1) `SectorLadderCard` rewritten as a compact stacked cell — top row: rank + ticker/label + RS-indexed % w/ tone + chevron; bottom row: z-score sparkline (160×20). Identity left-bar preserved. Card height now ~64px vs 76px row height of the wide ladder, fits 3-up on `lg:` viewports. (2) `SectorStrip` grid-cols-1/sm:2/lg:3 instead of vertical stack. Math contract unchanged (rsIndexed / rsMomentum / rsZScoreSeries / rsRankBySymbol / defensiveCrowding all preserved verbatim). (3) Always-on drill-in `RatioChart` + `SectorHoldings` at bottom — `useEffect` auto-selects the top-ranked sector on mount when no manual selection is present; clicking another sector swaps the active series. "Close" button removed (always at least one selected). (4) Yield-curve `RegimePanel` pin removed from the `SectorsTab` (operator's "redundant" call — it's already on the Overview tab). (5) New `lib/sector-cycle.ts` — `CyclePhase` type, `PHASES` metadata (4 quadrants w/ start/end angles + label + blurb), `SECTOR_PHASE` canonical mapping per Fidelity/SSGA taxonomy (XLY/XLF→early, XLI/XLK→mid, XLE/XLB→late, XLV/XLP/XLU→recession), `detectCyclePhase(t10y2y)` classifier — single-indicator: spread > +1.0 + steepening → Early, spread > 0 + flattening → Mid, spread ≤ 0 → Late, re-steepening from inversion within 12m → Recession. Falls back to `'mid'` on insufficient history. (6) New `lib/macro-views.ts` `SECTOR_IDENTITY_HEX` constant — hex literals paired with `SECTOR_IDENTITY_BG` Tailwind classes for SVG fill (classes don't apply inside `<svg>`). (7) New `components/macro/CyclePhaseWheel.tsx` — pure-SVG 360×360 donut wheel w/ 4 phase quadrants; active phase tinted at 0.18 plum opacity, inactive at 0.06 grey. Sector dots placed deterministically along the mid-radius arc within each canonical-phase quadrant (sorted alphabetically, 20% inset from quadrant edges). Dot radius linearly scales `rsIndexed` ∈ [80,120] → [6px, 16px]. Center callout: "you are here / `<phase>` / 2s10s `<spread>` · `<trend>`". Right sidekick: phase blurb + "How to read" interpretation guide. (8) `pages/Macro.tsx` `SectorsTab` restructured: wheel → grid → always-on chart (top to bottom). **Verification**: TS clean; Playwright `.audit/wheel-{sectors,detail}.png` show — current phase = Mid (highlighted top-right quadrant) inferred from 2s10s = +0.50 flat; XLK is the largest dot in the Mid quadrant (thesis-confirmed leadership: Tech canonically mid-cycle, +19.7% RS); XLE / XLB are medium dots in Late (out-of-phase commodity strength = investigate); XLV / XLU / XLP are small dots in Recession (canonical defensives NOT leading = no recession signal yet). Grid below renders all 9 cards in 3 columns with rank · RS-indexed · chevron · sparkline preserved. Drill-in chart auto-shows the top-ranked sector + holdings list at the bottom of the page. **Plan source**: `~/.claude/plans/now-when-i-use-radiant-yao.md` (Cycle Phase Wheel was originally deferred; operator opted-in).
- **Macro Sectors tab — RS Leadership Ladder + Yield-curve cycle linkage** (2026-05-17): operator brainstormed sector-relationship + cycle visualization via /product-brainstorming; 4-voice council converged on a single primary pick over RRG, heatmap, phase wheel, and correlation matrix alternatives. **What landed**: (1) `lib/macro-views.ts` extended w/ `SectorEtf` type (adds `defensive: boolean` per Fidelity/SSGA taxonomy — XLP/XLU/XLV flagged), `SECTOR_IDENTITY_BG` map (per-sector identity-color hue from the 2026-05-17 color taxonomy: XLK→liquidity slate, XLE→inflation ochre, etc.), and RS math constants (`RS_LOOKBACK_BASE=252`, `RS_ZSCORE_WINDOW=126`, `RS_MOMENTUM_WINDOW=14`, `RS_MOMENTUM_THRESHOLD=0.005`). (2) New `lib/sector-strength.ts` — pure-function math contract: `rsIndexed` (100 × RS(t)/RS(t-252) — sector-vs-SPY indexed to 1y base), `rsMomentum` (14-day rate of change), `momentumDir` (chevron direction w/ ±threshold dead zone), `rsZScoreSeries` (126-day rolling z-score for sparkline shape), `rsRankBySymbol` (descending cross-sectional rank, nulls sort last), `defensiveCrowding` (true when ≥2 defensives in top-3). All Close-only; reuses existing `useMacroRatio` cache; zero backend work. (3) New `components/macro/SectorLadderCard.tsx` — single ladder row: rank number (text-lg tabular-nums) · sector ticker+label (w-32) · z-scored sparkline (flex-1) · RS-indexed % w/ success-fg/danger-fg tone · ▲/▼/→ chevron. 4px identity left-bar matches Macro panel pattern. Selected state = `ring-2 ring-primary/40`. (4) `components/macro/SectorStrip.tsx` rewritten as the ladder — 9 stable-order `useMacroRatio` calls, `useMemo` derivations, sorted by rank w/ null-rank sectors at bottom, drill-in `RatioChart` + `SectorHoldings` preserved verbatim. Defensive-crowding cue: when active, ladder gets `bg-identity-stress/5` wash + 1-line caption. (5) `pages/Macro.tsx` Sectors tab now renders new `SectorsTab` wrapper that pins the Yield-curve `RegimePanel` (resolved via `REGIME_PANELS.find(p => p.title === 'Yield curve')`) above `SectorStrip` — the unique-to-this-app composition: cycle stage above, sector leadership below, one scroll. **Verification**: TS clean; Playwright `.audit/ladder-{sectors,drillin,drillin-full}.png` show — Yield curve panel pinned at top w/ 2Y/10Y/30Y rates + 2s10s/10y-3m spreads; ladder sorted descending by rank (current live order at 5y window: XLK +19.7% → XLE +10.9% → XLI −3.1% → XLB −6.2% → XLV −10.4% → XLU −12.0% → XLY −14.3% → XLP −15.4% → XLF −20.3%); momentum chevrons ▲ for XLK/XLE (gaining) and ▼ for the others (losing); identity-color left bars distinct per sector; click XLK → RatioChart drill-in + holdings panel renders below. **Skeptic-cleared declines logged in retro**: did NOT build 9×9 rolling-correlation heatmap (operator never asks "how correlated is XLE with XLU?"); did NOT adopt JdK RS-Ratio/RS-Momentum math (foreign vocabulary — vault has zero RRG content; the simpler indexed-RS + z-score gives ~80% of RRG signal at ~20% of cognitive cost); did NOT build Fidelity phase wheel (schematic, oversimplifies); did NOT add the ladder *alongside* the 9-cell grid (replaced, not accreted — single viz, single source of truth). **Deferred follow-ups** in plan file: rotation-footprint strip (12-week leadership trail); regime-conditional badges (each card's historical perf during current regime state); Cycle Phase Wheel. Plan file: `~/.claude/plans/now-when-i-use-radiant-yao.md`.
- **Research/Ask compaction round 3 + tooltip styling unification** (2026-05-17): operator audit on Research/Ask page — "unnecessary line spacing, extra text, options that could be condensed, elements that should share a line, tooltips using different font/formatting." **What landed**: (1) `InfoBubble` styling **unified across both modes** — the `content` variant now renders the same `space-y-2` wrapper as the `term` variant w/ an optional bold `label` row above the muted-foreground body; widths normalized to 320px. Both modes now look identical when hovered. (2) `AskInput` internal `space-y-3` → `space-y-2`; textarea `rows={3}` → `rows={2}` (operator types short questions; 2 rows is enough breathing room). (3) Submit button + keyboard hint **moved out of AskInput** entirely — collapsed into a single action row in `Research.tsx` alongside the Tickers input. Was: 3 vertical regions (AskInput action row w/ button-right, then Tickers row, then …). Now: one line `[Tickers (i)] [AAPL, MSFT input] [⌘/⌃+↵] [Ask button]`. Reclaims ~80px of stacked rows. (4) `Card` `CardContent` `space-y-4` → `space-y-3` on the Ask card — tighter rhythm consistent w/ the new 2-row textarea. **Dead code shipped**: AskInput's unused `useState`, `Loader2`, `Send`, `Button` imports stripped. **Verification**: TS clean; Playwright `.audit/research-tight.png` shows the entire Ask card collapsed to ~190px vertical (was ~450px+) — both Ask card AND History card header + filter chips + first 5 history items all fit in one viewport. **Decisions logged**: did NOT convert Skill chips to a dropdown (3 options is below the chip/dropdown threshold — chips are faster); did NOT convert Status filter chips in History (4 options, same reason); did NOT pull `<Textarea>` into the action row (textarea needs vertical canvas; sharing the line w/ button defeats the purpose). The "Scope: all [chips]" inline pattern stays as-is — already 1-line from a previous pass.
- **Research density + The Street IA collapse + Macro tooltip dedupe** (2026-05-17): operator's targeted screenshot-based audit on three pages. **Research/Ask**: (a) the bare `<input>` ticker field at the bottom of the Ask card blended into the page surface — swapped for the project's `<Input>` primitive (neumorphic `bg-background shadow-inset-sm` w/ focus ring) and the verbose `Tickers (comma-sep, optional — required only for hypotheses flagged requires_tv_context)` label promoted to an (i)-tooltip on the word "Tickers." (b) `Scope (optional — empty = all active)` 2-line block + chip row collapsed to a single line `Scope: all  [chips...]` — empty-scope semantics implied. (c) Keyboard-hint `⌘/Ctrl + Enter to submit. Latency ~3-8s.` reduced to a faint `⌘/⌃+↵` glyph next to the Ask button — operator already knows the latency. (d) `SkillPicker`'s always-on 2-line description prose ("Given a target ticker, identify a 4-7 name peer set…") + the "Verdict-only — won't propose hypothesis updates." warning both folded into a single (i)-tooltip on the "Skill" label. Dead `firstSentence` helper removed. **The Street IA collapse**: (a) `Ticker timeline` sub-tab + entire `components/the-street/TickerTimeline.tsx` deleted — redundant w/ the per-ticker Ticker Hub already reachable via the "Open Ticker Hub" link on each tier row. (b) With only `Latest tiers` + `Snapshot browser` left, the sub-tab strip removed entirely — latest tiers are the default page body. (c) Snapshot browser converted to a header-right `Snapshot` button that opens a `Dialog` at `w-[80vw] h-[80vh]`, w/ a native `<select>` dropdown of snapshot dates in the header row and the rendered `_index.md` body in an inset well below — `gap-2` (8px) between picker and body per "minimal line spacing" ask. Auto-picks most-recent snapshot on first open so the operator lands on content. `components/the-street/SnapshotPicker.tsx` deleted (functionality absorbed inline). **Macro tooltip dedupe**: the two adjacent (i)-icons next to the "Macro" title were redundant — kept just `<InfoBubble term="regime">` (canonical glossary definition); dropped the ad-hoc "About Macro" tooltip whose copy was chrome ("12 curated ratios + sector strip, updated nightly") + a discoverable interaction hint ("Click any row to expand…"). **Verification**: TS clean; Playwright `.audit/fix-{research,thestreet,snapshot-modal,macro}.png` confirm — Research/Ask is dramatically lighter w/ visible neumorphic ticker input + collapsed scope/hint rows; TheStreet header now just title + "Snapshot" button; modal opens at 80vw/80vh w/ minimal gap between dropdown and rendered _index.md body (verified content rendering "What this snapshot covers / Key counts / Cross-channel headline"); Macro shows a single (i) next to title. **Dead code shipped**: 2 components deleted (`TickerTimeline.tsx`, `SnapshotPicker.tsx`); `useStreetTicker` hook retained (cheap to keep; not auto-recoverable surface). All changes information-layer-untouched per hard-gate.
- **HoverTooltip viewport auto-flip + Decide board-chip sizing** (2026-05-17): operator surfaced two follow-ups after the card-unification pass. **Issue 1**: tooltips on top-of-page (i) icons (PageHeader, Card titles in the topmost row) were clipped by the browser's address bar because `HoverTooltip` defaulted to `side="top"` and used CSS-absolute positioning with no viewport awareness. **Fix**: `HoverTooltip` now measures the trigger via `getBoundingClientRect()` on every open and flips the requested side to its opposite when there's insufficient room — `top → bottom` when `rect.top < 216px` (200 estHeight + 16 margin); symmetric logic for `bottom/left/right`. Re-measured on every open so scroll/resize between opens doesn't strand a side. Verified: Macro page (i) icon at y≈103 now opens DOWN (was clipped behind topbar). **Issue 2**: Decide → Watchlist → Boards picker chips read `px-3 py-1.5 text-xs` — too dainty next to the adjacent Boards/Roster tab controls and the Opportunities tab buttons (`px-4 py-2 text-sm font-medium`). **Fix**: `pages/Watchlists.tsx` board chips upgraded to `px-4 py-2 text-sm font-medium rounded-xl` to match the Opportunities tab control vocabulary. Decide section now reads consistent across Signals/Opportunities tabs and Watchlist boards. **Verified**: TS clean; `.audit/tooltip-flip.png` shows page-header tooltip opening below the icon instead of being clipped; `.audit/boards-bigger.png` shows "Congress 4" and "positions 19" chips at full tab-control size. Tooltip auto-flip cascades to every InfoBubble in the app — no per-callsite props needed.
- **Think-section card unification — mirror Macro pattern across Research / Theses / TV Context / The Street** (2026-05-17): operator audit — "cards on TV Context, Street are still very poor for font sizing, spacing, styling. Cards across all tabs in Think should mirror the cards in Macro tab." **What landed**: Macro panel pattern (4px identity-narrative left-bar + `text-xl` CardTitle + `pb-1` CardHeader trailing gap) applied uniformly to every section-level Card across all 5 Think tabs. **Touched files** (10 cards across 8 files): `pages/Research.tsx` (Ask + History), `components/research/AnswerCard.tsx` (Verdict), `components/theses/ThesesList.tsx`, `components/theses/ThesisDetail.tsx`, `components/the-street/TierTable.tsx` (Tier 1/2/3/4 cards), `components/the-street/SnapshotPicker.tsx` (Snapshot browser), `components/the-street/TickerTimeline.tsx`, `pages/TVContextInbox.tsx` (Lookup ticker + Recent context cards). TVContextInbox page-header subtitle ("TradingView signals — webhooks, screenshots, notes, ideas, events.") also promoted to (i)-tooltip while there. **Deliberately left untouched**: pop-out form micro-cards (Note/Idea/Event input cards in TVContextInbox at L197/253/311) — they're inline form chrome triggered by buttons, not panels w/ headers; the `ContextItemCard` per-row item card — it's a list-item, not a panel; loading/empty-state placeholder Cards in `ThesisDetail.tsx` (no header to host the bar cleanly). **Single semantic chosen**: all Think-section bars use `bg-identity-narrative` (plum #7A5AA8) per the 2026-05-17 color taxonomy — narrative content = identity-narrative. Macro keeps its per-regime palette (inflation ochre, growth teal, etc.) because each panel is its own semantic category; the Think-section pattern uses the section-family color. **Verification**: TS clean; Playwright `.audit/think-{research,thestreet,tvcontext,tvcontext-2}.png` show full visual consistency — every Think card now has the same anchor bar + same `text-xl` title + same tight trailing gap as Macro panels. **Operator-visible**: Think section now reads as one cohesive surface instead of five different card vocabularies. Especially visible on TV Context (was `text-sm`/`text-base` mix — now uniform `text-xl`) and The Street (was `text-sm font-medium` — now `text-xl`).
- **Card-header consistency pass** (2026-05-17): operator audit — "card headers are inconsistent across the app; Macro is closest to good but can be 2× current; reduce line spacing between header end and rows by 60%." Found: 25 callsites override CardTitle to `text-sm` (14px — too small for non-strip cards); 14 use default `text-base` (16px including Macro); CardHeader bottom padding varies (`pb-2` × 22, `pb-3` × 4 on Macro, `pb-4` × 2). **Three surgical changes**: (1) `card.tsx` primitive `CardHeader` baseline tightened from `p-4 md:p-5` to `p-4 md:p-5 pb-2 md:pb-2` — default header→content gap collapses 16/20px → 8px **across every Card in the app** (zero per-card override needed). (2) `macro/RegimePanel.tsx` CardTitle bumped `text-base` → **`text-xl` (20px)** matching operator's "good but bigger" target; CardHeader override → `pb-1` (4px — 67% reduction from previous `pb-3` 12px). (3) Today 4-up cards (`DriftCard` · `FreshSignalsCard` · `ResearchCuriousCard` · `MarketMoodCard`) drop the `text-sm` CardTitle override — let primitive default (16px) win since these are full panels, not compact strips. Other 21 `text-sm` CardTitle callsites (`TickerReviewStrip`, `TVContextStrip`, `PendingReviewPanel`, theses-list rows, ticker hub mini-cards) **deliberately left as-is** — those are compact strips/lists where the smaller title is the right call. **Verification**: TS clean; Playwright `.audit/hdr-{macro,today}.png` show Macro panel titles substantially larger w/ much tighter trailing gap (Liquidity panel now fits 4 rows in same vertical space that previously took 5; Yield curve panel becomes visible above-the-fold). Today 4-up titles read slightly bolder; (i)-icons inline as designed. **Decisions logged**: did not raise primitive default beyond `text-base` (would have forced wide-blast text-sm callsites to upgrade or carry new overrides — bad ratio); did not touch the 21 strip/list `text-sm` callsites (intent-different from full panels — operator's complaint was about Macro-class panels, not strips); used `text-xl` not `text-2xl` for Macro (24px would have crowded the per-row InfoBubble icon + made the 6-panel grid feel top-heavy). Operator's "2x" read as visual hierarchy ask, not literal multiplier — `text-xl` reads ~2× because of the tighter trailing gap + bigger leading visual mass.
- **Density round 2 — page subtitles → (i)-tooltips + global gap tightening** (2026-05-17): operator returned w/ specific page-by-page complaints after the first density pass: Theses / Research / Macro page subtitles still always-on; Ask + History card descriptions still always-on; Macro "Window" label redundant next to time-range buttons; The Street snapshot badges stacked instead of inline-right; "very large spacing gaps and too many different font sizes." **What landed**: (1) `PageHeader` component **default behaviour flipped** — `description` prop now renders as an (i)-icon tooltip next to the title (was always-on `<p>` subtitle); `descriptionInline` opt-in escape hatch retained for the rare case the prose itself drives operator action. Cascade hit RxFinanceHypotheses, RxFinancePositions, every PageHeader caller in one edit. (2) `Theses.tsx` + `Research.tsx` + `TheStreet.tsx` use raw `<h2>` (not PageHeader) — wired their inline subtitles to the new `<InfoBubble content={...}>` pattern individually. (3) `Research.tsx` Ask + History card titles get (i)-icons; subtitle prose removed. (4) `Macro.tsx`: subtitle promoted to `<InfoBubble content=...>` next to title (in addition to existing `term="regime"` glossary bubble); **"Window" label dropped** entirely — 1Y/3Y/5Y/10Y/Max buttons are self-explanatory. (5) `the-street/TierTable.tsx`: CardHeader switched to `flex-row items-center justify-between space-y-0` so the "Snapshot YYYY-MM-DD" badge sits inline-right of the tier title instead of stacked underneath — reclaims ~28px per tier card. (6) **Global gap tighten**: 26 instances of `<div className="space-y-6">` (page-level wrapper) bulk-replaced to `space-y-4` across all `frontend/src/pages/*.tsx`. Page-zone gaps go 24→16px — measurable density gain (Macro now fits 4 full panels + part of the 5th in same viewport that previously showed only 4). **Verification**: TS clean; Playwright sweep on Theses/Research/Macro/The Street confirms subtitles hidden behind (i)-icons at h2 right edge, Snapshot badges inline-right on tier cards, "Window" label gone, page-zone spacing visibly tighter. **Skipped**: did not refactor `<h2>` callers in Theses/Research/TheStreet to use PageHeader (those pages have title-row layout requirements PageHeader doesn't accommodate cleanly — would have been wider blast radius for negligible gain since the `<h2>` inline pattern is fine once subtitles are tooltipped). Did not change Card-internal `space-y-N` (operator complaint was about page-zone spacing, not intra-card). **Operator-visible**: Theses now shows just "🧪 Theses (i) ... [6 active]" on a single tight line vs the previous 2-line title+subtitle stack — same for Research, Macro, The Street. Total reclaim ~120px above-the-fold across the Think section. Description prose still readable on hover for new operators / external auditors.
- **Primary recast (graphite ink) + type scale + density audit — 5 phases, council-driven** (2026-05-17): operator declared they didn't like the violet tint and that surfaces "tried to be spacious but in reality made the user squint by keeping too much text always on display in relatively small font." Convened visual-designer + UX-writer/typographer voices in parallel. Resulting plan executed in one sweep w/ audit-cycle per phase. **Phase A — Color recast**: visual designer's #1 pick **graphite ink `#2E3D52`** wins over Aged Bronze (collides w/ identity-inflation ochre) + Deep Petrol (3 teals risk muddying the system). Tailwind `violet` alias deleted; `--primary` CSS var rewritten to `213 28% 25%` (graphite); `primary-light #4A5C73` + `primary-active #1C2838` added under the existing shadcn `primary` block for hover/pressed treatments. K-logo breathing ring + section-tab active state both reposition naturally from violet → ink. Robust find-replace across 64 source files (BSD-sed POSIX `[[:<:]]`/`[[:>:]]` after discovering macOS sed `\b` is broken in `-E`) — every `*-violet*` class swapped to `*-primary*`; `bg-violet-light` → `bg-primary-light`. **Phase B — Type scale**: 4-tier canonical scale codified (`display 24/1.15` · `title 16/1.30` · `body 14/1.55` · `meta 12/1.40`); `CardTitle` standardized to `text-base leading-snug` (was `text-lg leading-none` w/ 3 callsite overrides); `CardDescription` default switched to `text-xs` (was `text-sm` — every callsite was already overriding); `CardHeader`/`CardContent`/`CardFooter` padding tightened to `p-4 md:p-5` (was `p-5 md:p-6`). **157 `text-[10px]` instances** upgraded to `text-xs` (kill-10px rule). **88 `uppercase tracking-wider` instances** stripped (no smallcaps under 14px rule — sacrificed ~30% legibility for "style"). **Phase C — Tooltip promotions**: `InfoBubble` extended w/ optional `content` + `label` props for ad-hoc tooltip body (additive; glossary-term path unchanged). Today 4-up cards (`DriftCard` · `FreshSignalsCard` · `ResearchCuriousCard` · `MarketMoodCard`) all lose their `CardDescription` prose to a `<InfoBubble>` next to the title — ~3 lines reclaimed per card. `RegimePanel` (Macro) blurb folded into existing glossary `InfoBubble` (when `term` exists) or inline `content` (when only `blurb`). **Phase D — RxFinanceDetail rework**: kill the 3-stat card-of-cards grid (drift/conf/created) — reclaim ~100px of vertical space — replaced w/ single inline meta-row `drift X.XX · conf YY · 3h ago` under the title chips. Local `Card label` helper retired. Twin CTA cards ("Ready to act?" + "Pattern worth tracking?" w/ subtitle copy) collapsed into a flat button row w/ `title=` hover tooltips. Forced-decision banner tightened from 2-line warning paragraph to single `[icon] Forced decision · snoozed N× — pick a disposition, don't snooze again` strip w/ palette colors (was off-palette `red-500`/`red-900`). **Phase E — Spacing rhythm**: snap to 4-unit ladder `{1,2,3,4,6}`. 33 `space-y-1.5` instances → `space-y-2`; 3 `space-y-5` instances → `space-y-6`. `space-y-{1,2,3,4,6}` left alone (each useful for their tier). **Verification**: TS clean after every phase; Playwright captured `.audit/p{A,B,C,Z}-*.png` showing graphite ink everywhere previously violet (K-logo, anchor bars, badges, primary chips), 4-up cards now title+(i)-icon w/ no inline blurb, macro panels w/ ink-toned tabs + tighter padding; Today page reads as instrument-grade quiet vs the previous violet vibrancy. **ui-components.md** gains a new "Type scale" + "Tooltip discipline" section + a new "Family 0 — Primary" entry under Color taxonomy explaining the recast. **Decisions logged**: kept `--chart-1` HSL at the old violet value (chart context separate from chrome; not auto-recoverable if changed); did NOT touch `text-xs`/`text-sm` body sizes (those are already the body/meta tiers); did NOT change page-zone spacing `space-y-6` (the only gap earning its keep); preserved invalid CardDescription overrides as harmless dead-noise (TS clean, future-pass cleanup). Information layer untouched per hard-gate.
- **Matte-bold color palette rollout — 6 phases, council-driven** (2026-05-17): operator wanted to break the all-grey UI w/ matte colors while keeping minimalism. Convened 3 council voices (visual designer + UX strategist + motion designer); synthesized into a 10-token palette across 3 families (semantic existing / identity new / motion new); operator approved full one-pass rollout w/ phase-by-phase audit + roll-back-later option. **Phase 1 (foundation)**: `tailwind.config.js` extended w/ 6 identity colors (`identity-{inflation,growth,liquidity,stress,narrative,ambient}` = burnt ochre / forest-teal / slate-blue / brick / plum / steel) + 3 wash colors (`wash-{success,snooze,dismiss}`) + `shadow-attention-ring` + 4 keyframes (`attention-pulse` 3.2s loop, 3× `disposition-wash-*` 320ms one-shot) + 4 animations. `ui-components.md` gains a full "Color taxonomy" section codifying the 3 families + 3 rules (max 1 dim per row · no new hex w/o named identity · color enters only when committed/crossed/cleared) + stays-grey list. **Phase 2 (Macro identity)**: `RegimePanel` gains a 4px identity left-bar; new `PanelIdentity` type discriminator + `IDENTITY_BAR_BG` map. Inflation→ochre, Growth→forest-teal, Liquidity→slate-blue, Stress→brick, Inflation regime→ochre (same family), Yield curve→slate-blue (rate family). Sparkline rate-stroke recolor + steel gridlines were council suggestions; **declined**: sparklines already encode delta direction via green/red — overriding to identity would lose direction info (rule #1 violation); Sparkline has no gridlines today so steel gridlines would be decoration (rule #3 violation). **Phase 3 (Theses two-channel)**: `RxFinanceHypotheses` time-to-expiry column upgraded from 2-tier raw Tailwind (text-red-600 / text-amber-600 — off-palette) to 3-tier on-palette ramp via `expiryClass()` helper: success-fg ≥30d / warning-fg 7-30d / danger-fg <7d / identity-stress expired. Invalidated hypotheses get a 3px identity-stress left-bar on the row + line-through on title. Status badge kept as primary status channel. **Phase 4a (Today icons)**: ResearchCuriousCard FileSearch icon migrated violet→identity-narrative; MarketMoodCard TrendingUp migrated violet→identity-ambient. DriftCard already used text-warning-fg conditionally (no change). FreshSignalsCard kept violet (operator primary action). **Phase 4b (Motion rec rail)**: new `RX_STATUS_BAR_BG` map in `RecRow.tsx` (open→violet, snoozed→ambient, acted→growth, dismissed→muted/40). 3px left rail on each rec — desktop table first-cell absolute overlay, mobile RecCard relative container; shared map = single source of truth. **Phase 4c (Trades win/loss rail)**: parallel pattern in `Trades.tsx` table + `TradeCard.tsx` — 3px left-bar on closed rows only (growth=win / stress=loss); open trades no bar (undecided state). **Phase 4d (TV Context provenance)**: `ContextItemCard` KIND_COLORS migrated from off-palette raw Tailwind (text-amber-500 / emerald-500 / sky-500 / rose-500) to on-palette tokens (warning-fg / violet / success-fg / identity-liquidity / danger-fg) — preserves visual identity for operator muscle memory while joining the rest of the matte-bold scheme. **Phase 5 (disposition motion)**: `RxFinanceDetail.tsx` fires `animate-disposition-wash-{success,snooze,dismiss}` on disposition POST 200 via TanStack `onSuccess` callback; one-shot 320ms ease-out overlay div (pointer-events-none, fixed inset-0, z-0) lerps card-bg to wash color and back. Three variants: acted_* → success-wash (sage); snoozed → snooze-wash (sand); dismissed → dismiss-wash (near-zero hue grey). **Phase 6 (K-logo ambient)**: new `hooks/use-inbox-total.ts` consolidates ticker_review + research + tv-context counts; new `KLogoLink` component in Layout.tsx applies `animate-attention-pulse` (3.2s ease-in-out infinite, violet ring 0.10↔0.22 alpha) when total > 0. Title tooltip + screen-reader label include count when nonzero. Peripheral-vision channel — one signal app-wide per motion-designer's rule. **Verification**: TS clean after every phase; Playwright captured `.audit/p9-0{2,3,4a,4b,6}-*.png` (Macro 6-bar grid · Theses ramp · Today 4-up tints · Motion rec sparse-data · K-logo at inbox=2). Motion/wash + win-loss bars verified-by-code only (no live recs/closed trades in seed DB to drive the visual). **Scope decisions logged**: declined to repurpose existing violet/success/danger semantics on surfaces that already used them well (DriftCard warning, FreshSignals violet, sparkline delta); kept StatusBadge as authoritative status channel on Theses (added expiry ramp + invalidated bar as orthogonal dimensions, not replacements). **Operator-visible**: Macro panels now read by regime at a glance (vs grey-on-grey muddle); rec list pre-attentive disposition rail; trade list pre-attentive win/loss rail; TV Context source dots on-palette; disposition POST gets a body-receipt wash; K-logo breathes when inbox nonzero. All changes tokenized — easy to tune hex values + animation timings retrospectively. Information layer untouched per hard-gate.
- **Topbar section-aware tabs + translucent treatment** (2026-05-17 — follow-up to nav re-imagination): operator asked that the rail's drawer-on-click pattern be complemented by an always-visible section tab strip in the topbar so they don't need to re-open the drawer to flip between Signals/Predictions/Macro/Watchlist (Decide) or Research/Theses/TV Context/The Street (Think). Leaves (Today/Admin/Docs) keep the breadcrumb-only treatment. **What landed**: new `findCurrentSection(pathname)` helper resolves whichever section's child route you're on; new `TopbarSectionTabs` subcomponent renders `[Icon] SectionName | tab tab tab tab` with the active tab in violet inset; topbar background lifted to `bg-background/60 backdrop-blur-md` w/ a thin `border-b border-border/40` underline so the body reads as a separate plane underneath; px reduced from `md:px-8` → `md:px-6` to make room for tabs. **Doesn't break leaves**: breadcrumb still shown for `/`, `/admin`, `/docs`, and deep-links like `/ticker/:symbol`. **Drawer behavior unchanged**: clicking the Decide/Think rail icon still opens the slide-out drawer for cross-section discovery; the topbar tabs are the fast in-section switcher once you've landed. **Verification**: TS clean; Playwright captured `.audit/p5-01-decide-topbar.png` (Macro active w/ Signals/Predictions/Watchlist siblings), `p5-02-think-topbar.png` (Research active w/ Theses/TV Context/The Street siblings), `p5-03-admin-leaf-topbar.png` (leaf preserves breadcrumb-only); in-topbar click `predictions → macro` works end-to-end.
- **Nav re-imagination + Railway shutdown + legacy dashboard removal + thesis health bugfix** (2026-05-17): operator asked for 4 things in one pass — drop the dual-backend top ribbon (Railway permanently retired), drop the legacy `/admin/overview` Dashboard since Admin is now one panel, replace the persistent 240px left sidebar w/ a navigation drawer pattern (no collapsibles, flat sections), and fix the empty Hypothesis Health surface. **Bug fix (Phase 1)**: the "empty thesis health" was actually `/v1/hypotheses/health/list` 500ing because migration `0030_recommendations_attention` (adds `recommendations.attention_score` + `attention_breakdown`) had never run on the live DB — sidebar widget calling `/summary` was always fine. `alembic upgrade 0029 → 0030`; `RxFinanceHypotheses` page now renders 6 active hypotheses w/ age + days-to-expiry. **Phase 2 (Railway shutdown)**: `BackendToggle.tsx` deleted; `BackendId` collapsed to single-member union `'laptop'` (kept the type rather than sweeping ~80 `backendId` cache-key plumbing call sites in `use-api.ts` — defensive); `backend-store.ts` reduced to one entry + collapse-to-laptop getters; dead-branch `LaptopOnlyBanner`s in `Research.tsx` + `TVContextInbox.tsx` + the Railway warning block in `CadencesTab.tsx` all removed; `BackendHealthBanner` no longer offers a switch button; `vite-env.d.ts` Railway env types stripped. **Docs sweep**: new ADR `018-railway-shutdown.md` is the canonical record; `CLAUDE.md` Entry points line + "Setup (Railway)" section flagged as removed; `railway-deployment.md` gets a top banner; `frontend/api-client.md`, `architecture.md`, `pages.md`, `ui-components.md`, `guides/architecture.md`, `guides/README.md` updated to point at ADR 018. Module-level docs not deep-swept — historically-accurate, cross-ref via ADR. **Phase 3 (legacy dashboard)**: `pages/Dashboard.tsx` + `components/dashboard/{RegimeStrip,LatestOpportunity,AccuracyTile}.tsx` all deleted (zero external imports — confirmed via grep); `/admin/overview` route in App.tsx now redirects to `/admin` (back-compat for bookmarks); Today.tsx 7-line "Looking for the legacy dashboard?" footer + unused `Link` import stripped; sidebar leaf entry removed from NAV_GROUPS. **Phase 4 (nav re-imagination)**: operator chose "persistent icon rail + drawer-on-click" (vs slide-in hamburger or flat-no-drawer); rewrote `Layout.tsx` from 308 LOC → 268 LOC w/ new IA primitives `NavLeaf` / `NavSection` / `NavChild` (`isLeaf` typeguard gone, kind-tagged unions now). 56px icon rail always visible (K-logo home + 5 section icons: Today/Decide/Think/Admin/Docs); clicking a leaf icon navigates direct + closes any open drawer; clicking a section icon (Decide/Think) toggles a 240px slide-in drawer w/ section heading + flat list of children + `RxStatusWidget` + `HypothesisStatusWidget` pinned at bottom; drawer auto-closes on navigation, on Esc, and on backdrop click. Collapsibles removed entirely (no `useState`/`localStorage` per-section collapse state, no auto-expand surprises). Admin demoted to a leaf since legacy dashboard is gone. Topbar shrunk from 64px → 56px and lost the hamburger menu (rail is always-visible). **Verification**: TS clean across all 4 phases; backend tests have a pre-existing 403 auth-fixture failure unrelated (confirmed via git-stash regression — same failures with my work reverted); Playwright at 1440×900 captured `p4-01-today-rail.png` (rail + Today canvas), `p4-02-decide-drawer.png` (Decide drawer w/ Signals/Predictions/Macro/Watchlist + THESES 6 widget at bottom), `p4-03-macro-closed.png` (drawer auto-closed on nav, Macro renders with breadcrumb + LineChart icon active in rail), `p4-04-theses-health.png` (6 active hypotheses w/ status badges + age + expires-in — bug fix proof), `p4-05-admin-direct.png` (Admin leaf navigates direct w/o drawer, Zap icon active). **Information layer untouched** per hard-gate. **Ledger / declines**: kept `app/sync/*` backend module + `SyncOutbox` table in place even though Railway is the only peer (would invalidate existing rows; dormant when no peer URL — harmless); kept `BackendId = 'laptop'` as a single-member union rather than gutting `backendId` plumbing (~80 call sites — sweep risk > benefit); kept Dockerfile + tailscale-entrypoint.sh + railway.toml in-repo for possible re-deploy; module-level docs not deep-swept (ADR 018 + top-level doc updates declared sufficient — moduledocs are historically accurate).
- **tv-context decision-engine enrichment Phase 3 — hypothesis invalidator DSL reads `HypothesisTVContextLink`** (2026-05-17): closed the third (deepest) of 3 gaps where TV-context inputs landed in `tv_context_items` but never reached downstream decision systems. New invalidator DSL ops `tv_context_count_since(days, min_count)` + `tv_context_stance_count_since(days, stance, min_count)` in `app/hypotheses/invalidator.py`. Validator + dispatch updated; `evaluate()` signature extended to `evaluate(spec, *, session, hypothesis_id=None)` — backwards-compatible because macro ops ignore the new param. `run_daily_tick` in `app/hypotheses/service.py` passes `hypothesis_id=row.id` so the new ops can resolve their link table. Operator flags a screenshot as `stance="challenges"` on a hypothesis → 2+ in 14d → status flips to invalidated next tick. **Ledger**: picked option A (new DSL ops) over option B (parallel context-eval pass) — operators already author invalidators in DSL form, so adding DSL ops keeps everything operator-tunable via existing schema. Stance vocab correction: plan said `evidence-against` but actual model uses `('supports', 'challenges', 'context')` — operator-facing "evidence against" maps to `stance="challenges"`. Soft-skip on missing hypothesis_id: tv-context op without id returns `fired=False` with descriptive reason instead of raising, mirroring the rest of the DSL's "insufficient input never crashes the daily tick" pattern. **Deferred**: stance form param on note/idea ingest routes (plan asked for a dropdown — link table already accepts stance on insert, operator can stamp via screenshot-link path) + hypothesis-detail "Recent linked context" frontend surface (rx-finance attention badge already gives the operator-attention feedback loop). 9 new tests in `tests/test_hypotheses.py`; 847 backend pass (+9 from 838); same pre-existing TZ-flaky earnings-trigger test unaffected. **All 3 phases of `tv-context-decision-engine-enrichment` SHIPPED**: ticker_review parity (Phase 1, c08a3d2) → rx-finance attention axis (Phase 2, 44da743) → hypothesis invalidator DSL (Phase 3, this commit). End-to-end: drop a `$PLTR` screenshot → ticker_review_queue gets a row → promote PLTR → next rec mentioning PLTR shows attention_score > 0 with violet badge → if operator flags a screenshot as 'challenges' against an existing hypothesis, next `_hyp_tick` flips status on threshold.
- **tv-context decision-engine enrichment Phase 2 — rx-finance attention axis** (2026-05-17): closed the second of 3 gaps where TV-context inputs landed in `tv_context_items` but never reached downstream decision systems. New `app/rx/tv_context_signal.py:compute_attention_for_rec()` extracts tickers from rec `tldr+body_md` (re-using `_TICKER_NOISE_DENYLIST` from `links_for_rec`), queries each ticker's last-14d items, applies locked kind weights (screenshot=1.0 / note=0.7 / idea=0.5 / event=0.4 / webhook=0.2) × `exp(-ln2 × age_d / 7)` decay. Migration `0030_recommendations_attention.py` adds `attention_score FLOAT NULL` + `attention_breakdown JSON NULL` (additive; null on legacy rows). `service.create` stamps both at rec creation; `list_recs` + `GET /v1/rx/recs/{id}` surface them. Frontend gains `RxAttentionPerTicker` type + violet "👁️ Operator attention" tile on `RxFinanceDetail.tsx` showing per-ticker count breakdowns ("NVDA: 3 screenshots + 1 note · META: 2 notes"). **Ledger**: design picked option B (explicit attention column) over option A (composite-score modulation) — decision-engine changes that aren't visible are anti-product. Top-level score = MAX across tickers, not SUM, so a rec mentioning 2 tickers where one is heavily-discussed doesn't get diluted. Best-effort contract: compute failure NEVER blocks rec creation (try/except + log; row commits with null attention fields). Trade-off: persisted-at-create means attention is stale if operator screenshots arrive after rec creation — accepted for v1 because recs are short-lived + avoids per-list-read N+1 queries. Future: optional `?refresh_attention=true` query param on detail endpoint. Surprise: rec has no `ticker` field (single rec can reference multiple tickers via prose), so leaned on existing denylist regex to extract them. 11 new tests; 838 backend pass (+11 from 827); frontend `tsc --noEmit` clean. Next-phase implication: Phase 3 will need similar ticker-extraction from hypothesis `claim_text` for the DSL ops, but `HypothesisTVContextLink` already provides the explicit link so extraction may not be needed.
- **Follow-up gap-closure: 4 post-audit gaps + Cadences cost column** (2026-05-17): operator's highest-impact post-audit gap list, shipped autonomously. **Gap #2 — Theses prefill from rec**: `Theses.tsx` now consumes `?from_rec=<id>&ticker=<sym>` query params, renders a violet-tinted banner above the status filters w/ rec short-id link back + ticker chip. Substring-matches the ticker against active hypothesis titles — click-to-select chips if matches exist, vault-author hint (`~/Documents/Sho's Playgroun/Lakshmi/02_library/theses/<slug>.md`) if not. Dismiss button strips the query params. Kills the dead-link from "Promote to thesis" CTA. **Gap #5 — Mobile `TradeCard`**: new `components/trades/TradeCard.tsx` mirrors RecCard pattern (md:hidden stack + hidden md:block table). 3-col grid for qty/entry/exit, colored P&L line (success-fg/danger-fg semibold), related-rec link, Close button for open trades. Completes the mobile rec-trade flow that RecCard started. **Gap #6 — TradeForm `entry_at` backdating**: new "Entry date" input (HTML date, max=today) defaulting to local today. Onsubmit composes midday UTC ISO (`<date>T12:00:00Z`) — keeps local-day intent stable across DST + TZ rounding. Operator can retroactively log trades for past dates without DB hacking. **Gap #3 — `useTVContextRecent` hook + `GET /v1/tv-context/recent` endpoint**: new backend route + service fn `list_recent` (default 50 items, active only, optional kinds CSV, ordered by captured_at desc). `InboxCounter` on Today now includes "TV context" chip alongside "Tickers to review" + "Research approvals" — replaces per-ticker fan-out that was previously impossible. **Cadences cost column**: new column on `CadencesTab` maps `loop_id → cost_driver` via `LOOP_COST_DRIVER` constant (currently `research_weekly` → research). For cost-sensitive rows: `$X.XX /mo · research` from `useCostsMonthly`. For others: `$0`. Footnote below table calls out TV-context vision is HTTP-triggered (not cadenced) and links spend back to Costs tab. **Verification**: TS clean; 85 tv_context+trades+admin backend tests pass; Playwright walked `/theses?from_rec=test&ticker=AAPL` (banner + vault-author hint), `/motion/trades` Log-trade modal (Entry date 17/05/2026 prefilled), `/motion/trades` at 400×900 mobile (NVDA closed +$56.60 + AAPL open stacked cards), `/admin/cadences` Research-weekly row showing `$0.09 /mo · research`. Information layer untouched. New cost-sensitive loops added later need a one-line entry in `LOOP_COST_DRIVER`.
- **Macro yield-curve rework + positions board seed** (2026-05-17): operator-driven dual task. **Task A**: extracted 19 US-tradable tickers from `Lakshmi/02_library/positions_ledger.md` (AMZN, META, MSFT, ADBE, IBKR, AZO, MA, NKE, PYPL, VFF, MSTR, OXY, IJS, KR, UNH, PATH, BABA, OKTA, PLG; skipped INDIA-ETF placeholder + BTC/ETH crypto + PENSION + receivables + RTY-vehicle-TBD) → created `positions` board in TradingV w/ all 19 via `POST /v1/boards/{id}/tickers`. **Task B**: vault-audit of `Videos/{click-capital, fx-evolution-daily}` transcripts surfaced repeatedly-referenced macro signals (10y/30y treasuries, 2s10s curve, MOVE bond-vol, DXY, VIX, copper, oil, HYG/credit). Gap analysis vs existing macro registry found 4 missing series: **WGS30YR** (30Y, operator's "all-important 5%" level), **T10Y2Y** (2s10s spread, recession indicator), **T10Y3M** (10Y-3M, NY Fed model input), **^MOVE** (ICE BofA bond-vol, operator-flagged leading indicator). Council deliberation captured in `.claude/decisions/macro-yields-rework-2026-05-17/00-audit-and-plan.md`; phased plan executed: **Phase 1** registry.yaml += 4 symbols, backend restart, macro refresh ingested all 4 (48 ok / 0 failed); **Phase 3** macro-views.ts gains 6th "Yield curve" REGIME_PANEL w/ 5 rows (2y/10y/30y/2s10s/10y-3m) + Stress panel gains MOVE row beside VIX (VIX relabelled "VIX (equity vol)" for disambig); **Phase 4** glossary.ts += 6 entries (`yield_curve_axis`, `r_wgs2y`, `r_wgs30y`, `r_t10y2y`, `r_t10y3m`, `r_move`) w/ operator-tuned `directional.threshold` per project convention; reused `r_wgs10y` from Liquidity panel (no dup). **Verification**: 21 macro tests pass; TS clean; Playwright `.audit/macro-01-with-yield-curve.png` shows 6 panels w/ correct data (30Y=4.97, 2s10s=0.50, 10Y-3M=0.90, MOVE=79.87); click-test on 30Y row expands to 5-year line chart (`.audit/macro-02-30y-expanded.png`). **Skeptic-declined**: multi-line yield-curve chart (3 rates same-axis — defer until operator asks); BTC on macro (operator-deferred); Tom Lee specific price targets (narrative, not macro). Information layer untouched per hard-gate. Per-phase decisions log: `.claude/decisions/macro-yields-rework-2026-05-17/log.md`.
- **tv-context decision-engine enrichment Phase 1 — ticker_review parity for note/idea/screenshot/event** (2026-05-17): closed the first of 3 verified gaps where TV-context inputs land in `tv_context_items` but never reach downstream decision systems. New `_maybe_enqueue_review(ticker, kind, snippet)` helper in `app/tv_context/service.py` wired into `ingest_note` / `ingest_idea` / `ingest_screenshot_row` / `ingest_event` — when an operator submits a TV-context input with a ticker NOT in `roster ∪ boards ∪ The Street tier-1/2`, that ticker now lands on the same `ticker_review_queue` strip the video pipeline writes to. **Webhook ingest intentionally skipped** — webhook payloads are rule-driven so the ticker is pre-filtered by the operator's alert config; bulk-enqueueing would burn the Today strip with false positives. **Best-effort contract**: a queue write must NEVER block / fail the host ingest, so the helper wraps everything in `try/except + log` and short-circuits when `SETTINGS.TV_CTX_TICKER_REVIEW_ENABLED=False`. **Test gating**: SQLite serial write-lock semantics deadlocked existing tv_context tests when the inner ticker-review session tried to commit mid-ingest, so `conftest.py` flips the env-var default OFF and Phase 1's 5 new tests monkeypatch SETTINGS back to True. **Ledger**: design picked async path (`await load_ticker_whitelist()` + `await enqueue_or_bump()`) over the plan's literal `_sync` wording because TV-context routes are async; trade-off was an extra settings flag for the SQLite test-isolation edge case. Surprise: SQLite's `:memory:` serial write lock blocks the inner session indefinitely when the outer ingest still holds an uncommitted transaction — production (Postgres) has no equivalent issue. Next-phase implication: same async-vs-sync seam exists in `app/rx/` so Phase 2's `compute_attention()` will use async DB reads. 5 new tests; 827 backend tests pass (1 pre-existing TZ-flaky earnings-trigger test unaffected).
- **rx finance v1.x.1-d — UX audit + 15 enhancements before operator's first real trade** (2026-05-16): operator declared they'd start logging real trades the next day; ran a deep visual + council audit to surface friction before the workflow goes live. **Playwright-driven screenshot pass** of Today, Motion/recs, Motion/positions, Theses/health, rec detail, and Trade form caught a P0 routing bug (`/motion/recs/:id` was rendering the Opportunities tab because `useParams` couldn't bind `tab` when the path had a literal `recs` segment — fixed by detecting detail mode via `id` presence alone) and the markdown body rendering completely flat-text (the page used Tailwind's `prose` class but the typography plugin isn't installed; fixed by switching to the project's existing `.docs-article` CSS class). Council deliberation (architect/pragmatist/critic/skeptic/designer) produced a ranked plan of 15 items. **All 15 shipped**: P0-A markdown body now renders headings + lists + bold properly via `.docs-article`; P0-B `unrealized_pnl` + `unrealized_pnl_pct` per position + `portfolio_unrealized_pnl` aggregate on `/v1/trades/positions`, surfaced as 3-card header + colored P&L column on the Positions table; P0-C "Log trade from this rec" CTA on rec detail deep-links to `/motion/trades?rec=<id>&ticker=<auto-extracted>` (uses same `_TICKER_NOISE_DENYLIST` as backend service to avoid prefilling "BUY"/"USA"), prefills Ticker + Notes + Related rec — collapsing the rec→trade flow from 6 clicks to 1; P0-D `Rec` column on Trades table (violet short_id link → rec detail); P0-E risk-flag suppression `RISK_FLAG_MIN_PORTFOLIO=$5k + RISK_FLAG_MIN_POSITIONS=4` so a sparse book doesn't fire every row (cry-wolf fix); P0-F TLDR + body combined into one card with violet left-border treatment for TLDR (reduces detail-page scroll); P1-G `fmtCur` consistent 2-decimal currency formatting across all surfaces; P1-H `auto-revived` badge now amber w/ semibold weight (visually distinct from plain yellow `open`); P1-I hypothesis-health "Recent recs" column hidden when sum=0 across all rows (dead-UI suppression); P1-J trade-form rec dropdown enriched to `short_id · drift X.XX · status · tldr`; P1-K Trades table realized P&L uses `text-success-fg`/`text-danger-fg` font-semibold; P2-L disposition panel reordered to **buttons-first → fit → note → snooze** (operator decision flow: decide, then annotate); P2-M new `RxStatusWidget` in sidebar bottom (open recs awaiting + forced count, hidden when zero, click → `/motion/recs`); P2-N RxStrip dropped duplicate temporal info (single colored relative-time string replaces "16d ago" + "aging 16d" pair); P2-O disposition button palette uses theme `bg-success` / `bg-success/70` / secondary / ghost (replaces ad-hoc green-600/green-500 hex). **End-to-end smoke**: seeded 4 synthetic recs (open, forced-snoozed/auto-revived, fresh, terminal-acted) + 2 open trades + OHLCV closes for AAPL+NVDA → captured before-screenshots → shipped fixes → captured after-screenshots → exercised rec detail → "Log trade from this rec" → form prefills `ticker=AAPL, related_rec_id=fc85da92, notes="From recommendation fc85da92"` → POSTed via API → trade lands w/ FK → Trades table renders rec linkage as clickable violet short_id → cleanup dropped all synthetic rows. **Backend tests**: 721 pass (was 716 + 4 new in `test_rx_links_and_positions.py`: `test_positions_risk_flag_requires_min_positions_and_portfolio` replaces old single-flag test, `test_positions_risk_flag_fires_when_thresholds_met` covers the new gated behavior, `test_positions_includes_unrealized_pnl` validates new field). Plan + audit findings captured inline in this session; new module-doc + roadmap retro entry land here. **Operator-ready for tomorrow**: open Today → see RxStrip + sidebar widget → open rec → read formatted body → click "Log trade from this rec" → prefilled form → Save → trade appears in Trades w/ rec link visible → P&L populates on Positions as soon as the next OHLCV tick lands. Detail back-link uses "Back to recommendations" label, currency consistency across all surfaces, no cry-wolf on risk flags when book is sparse.
- **UX rework — 7 phases, council-driven holistic visual + IA sweep** (2026-05-17): operator declared trades start tomorrow + asked for deep council + autonomous full-workflow execution. Spawned 4 parallel council agents (UX strategist + visual designer + frontend architect + skeptic) in `.claude/decisions/ux-rework-2026-05-17/`; synthesized into 7-phase plan; shipped all phases with per-phase audit-cycle protocol (TS check → backend tests → Playwright screenshot diff → log entry → skeptic-check on declined work). **Phase 1 foundation primitives**: `DriftBar` (unified 4 drifted implementations w/ aria meter + size variants); `StatusBadge` (single vocabulary across recs/hypotheses/trades/opportunities/positions/flags — replaces 3 dialects); `TabbedShell` (replaces hand-rolled tab boilerplate on Motion + ThesesShell + Predictions, ~300 LOC reclaimed). **Phase 2 card vocabulary**: KPI cards inverted from inset → extruded per designer P1-5 (Positions/Trades/Detail); `PageHeader` upgraded w/ violet anchor bar + 3xl extrabold + icon slot; `tabular-nums` enforced on hero metrics. **Phase 3 Today reshape**: 4-zone layout (Action queue / Inbox aggregate / What changed 4-up grid / Inbox detail strips); new `InboxCounter` row replaces 3 separate vertical inbox surfaces — total scroll reduced ~40% at desktop, mobile single-column still works. **Phase 4 sidecar pattern**: `PageWithSidecar` + `SidecarTile` primitives; `RecsSidecar` (disposition funnel + attention flags + next-batch hint) + `PositionsSidecar` (concentration top-5 + cost-vs-value w/ unrealized P&L + risk thresholds reference) kill the ~600px empty-grey-clay below tables. **Phase 5 rec/trade ergonomics**: mobile-stacked `RecCard` for <md viewports (UX B5); Context column on rec list w/ "IN POSITION" / "ON WATCHLIST" annotations; client-side conflict detector (trim/close/sell verb on a held long); default sort `forced > aging > conflicts > drift`; rec detail gains 2-up CTA row (Log trade + Promote to thesis); snooze history section per-rec. **Phase 6 sidebar + IA polish**: RxStatusWidget + HypothesisStatusWidget compacted to ambient text-only strips (designer P2-9 — sidebar no longer brightest objects); top-level `/recs` + `/recs/:id` + `/trades` shortcuts redirect into canonical Motion tabs (UX strategist O8 — easier-to-type URLs for high-frequency surfaces). **Phase 7 architect cleanup**: `LazyRoute` helper replaces 7 of 11 per-route Suspense+Skeleton wraps; `useUrlState` hook scaffolded for opt-in URL-state migration; App.tsx LOC 200 → 149; dup `/trades` route deleted. **Skeptic-cleared declines**: did NOT split `use-api.ts` (1832-LOC monolith — barrel works, regression risk vs marginal cohesion gain); did NOT adopt `openapi-typescript` CI (would force backend Pydantic tightening — soft-gate violation); did NOT extract DataTable primitive (premature without ≥3 sites adopting side-by-side); did NOT rename Decide/Think sidebar groups (muscle memory cost); did NOT redesign Trades.tsx workflow (frozen until operator has ≥1 week of real trade entries per skeptic). **Verification**: TS clean across all phases; 727 backend tests pass (1 pre-existing `test_earnings_trigger_polling` failure unrelated — confirmed via git-stash regression); Playwright walk-through captured at `.audit/p{1-7}-*.png` + `.audit/final-01-today.png`; rec→trade e2e re-verified post each phase. **Decision log**: `.claude/decisions/ux-rework-2026-05-17/log.md` w/ per-phase ship-list + skeptic-check + gap-callouts. **Operator-visible**: scan-Today in <1 viewport at 1440; mobile rec list doesn't side-scroll; conflict/position annotations on rec list highlight which recs actually affect the operator's book; sidecars fill former empty-clay with disposition-habit + concentration narratives.
- **rx finance IA integration — fold into Motion + Theses + Today strip** (2026-05-16): v1.x.1-a/b shipped rx pages as orphan top-level `/rx-finance*` routes with no sidebar entry — operator could only reach them by typing the URL. Refactored into existing IA along the operator's job boundary: rec list + detail + positions become tabs of `Motion` (`/motion/recs`, `/motion/recs/:id`, `/motion/positions`) since they're action surfaces alongside opportunities + trades. Hypothesis health becomes a tab of `Theses` (`/theses/health`) since it's thesis hygiene. Today gets a new `RxStrip` between `TickerReviewStrip` + `TVContextStrip` showing top-3 open recs ranked forced-decision > aging > drift_score desc, hidden when no open recs exist (operator-respectful empty-state). Legacy `/rx-finance*` routes redirect into the new paths (`LegacyRxDetailRedirect` preserves `:id` param). New `pages/ThesesShell.tsx` wraps existing `Theses` w/ tab UI without modifying the existing page. New `pages/Motion.tsx` extends from 2 to 4 tabs + short-circuits the shell when `:id` present (renders detail standalone). `RxFinance.tsx` lost its subnav chips (now tabs in parent shell), `RxFinanceHypotheses` + `RxFinancePositions` lost their `← Recs` back-links. **Council rationale logged in `.claude/plans/rx-finance-ia-integration.md`**: didn't introduce a 6th sidebar group because the rec list is single-tab + the data is a projection over Motion+Theses already; tabbing into existing groups respects the established IA pattern (Predictions, Macro, Motion all use tabbed shells). All 7 routes (`/motion/recs`, `/motion/recs/:id`, `/motion/positions`, `/theses/health`, plus 4 legacy redirects) verified via Vite proxy + backend; click-through from RxStrip → detail page → back-link → list confirmed working. Backend untouched. Plan: `.claude/plans/rx-finance-ia-integration.md`.
- **rx finance panel v1.x.1-b — TradingV-native cross-references: hypotheses, positions, trade linkage** (2026-05-16): additive on v1.x.1-a. Three new read endpoints + extends `POST /v1/trades` w/ optional `related_rec_id`. `GET /v1/hypotheses/health/list` returns each hypothesis with age, days-to-expiry, and a count of recent (last 30d) finance recs whose `tldr|body_md` mentions the title (case-insensitive substring, min-length-3). `GET /v1/trades/positions` aggregates open trades into per-ticker positions w/ qty, avg_price, current_value, %portfolio + `risk_flag_single` (>5% of book) flag. Single grouped OHLCV query (no N+1 — preferred daily bar via `(symbol, MAX(ts))` subquery + JOIN; any-interval fallback). Trade rows materialized to dicts inside the session block to avoid SQLAlchemy async lazy-load on detached instances. `GET /v1/rx/recs/{id}/links` resolves cross-references: hypotheses by title substring + trades by explicit FK (`related_rec_id`) UNION ticker substring (regex `\b[A-Z]{2,5}\b` + `_TICKER_NOISE_DENYLIST` strips noise tokens like USA/GDP/FED/BUY/IPO/CEO; trade match bounded to open positions OR closed within last 90d). `POST /v1/trades` extended w/ optional `related_rec_id`; service validates the rec exists + is `domain='finance'` before stamping FK. **Frontend**: two new lazy-loaded pages (`RxFinanceHypotheses.tsx` + `RxFinancePositions.tsx`); detail page gets inline Hypothesis + Trade cross-reference cards via `useRxLinks`; list page gains subnav chips to Hypotheses + Positions; `Trades.tsx` form gets optional `related_rec_id` autocomplete (filters to finance recs from last 30d). Route registration order: static segments (`/rx-finance/hypotheses`, `/rx-finance/positions`) before dynamic `/rx-finance/:id`. **Audit-cycle protocol applied**: code-reviewer flagged 3 HIGH (route shadow — invalid, verified via live request; N+1 OHLCV — fixed via grouped query; ticker regex false-positives + no recency bound — fixed via denylist + 90d cutoff) + 3 MEDIUM (session contract inconsistency in list_health — accepted, low impact; post-session ORM access in list_positions — fixed by materializing to dicts; naive-vs-aware datetime — accepted, SQLAlchemy 2 + TIMESTAMPTZ handles both cleanly, test-verified) + 2 LOW (skipped). 17 new tests in `test_rx_links_and_positions.py` (hypothesis health empty/counts/min-length, positions aggregation/fallback/risk flags/excludes-closed/rec-link, trade create w/ rec FK + unknown-rec reject, links via FK + ticker-substring + 404; **3 audit-driven tests**: denylist denies noise tokens, 90d cutoff excludes old closed trades, batched OHLCV query works for many tickers). Full backend suite **719 pass** (was 702 + 17 net new tests this commit; rx coverage is 26 + 17 = 43 tests for the layer). Plan: `.claude/plans/rx-finance-panel.md` Phase v1.x.1-b. **Known limitations**: (1) substring-based hypothesis-rec linkage = false-positive prone; future = explicit FK on rec → hypothesis. (2) Sector concentration risk-flag NOT computed (no sector table in TradingV); flag stub always returns False — explicit gap for v1.x.1-c. (3) `current_price` falls back to entry-price when no OHLCV cached for ticker. **Phase I unlock criteria** (per brief): ingestion endpoint live ✓, operator can read+disposition ✓ (v1.x.1-a), `/rx-finance` rewired to TradingV (pending operator + rx-layer agent coordination), trade-w/-rec-link form ✓ (v1.x.1-b), `position_thesis_match` reads real position data ✓ (positions endpoint ready), risk-rule checks fire ✓ (single-position threshold). Operator + rx-layer coordination = final cutover task; log D-046 on completion.
- **rx finance panel v1.x.1-a — TradingV becomes exclusive surface for finance recs** (2026-05-16): closed the D-045 storage-routing lock by giving TradingV its own `recommendations` table + ingest endpoint. Lovable/Supabase already owned fitness + nutrition; this commit lands the symmetric leg for finance. Migration `0029_recommendations.py` creates the table (UUID-string PK matching `trades.id` pattern; columns mirror Lovable Supabase schema so the laptop's `/rx-finance` slash command can dual-write without per-domain branching). Three CHECK constraints: `domain = 'finance'` (defense-in-depth above the defensive `WHERE` filter in every service read), `status IN ('open','snoozed','acted','dismissed')`, `subjective_fit_1_5 BETWEEN 1 AND 5`. Three indexes: `(status, created_at DESC)`, `(snoozed_until) WHERE NOT NULL`, `(owner_user_id)`. Same migration adds `trades.related_rec_id` nullable FK ON DELETE SET NULL — Phase B unlock for the `position_thesis_match` signal in `/rx-finance` once trade-capture form lands. New `app/rx/` module: `models.py` (ORM mirrors migration), `schemas.py` (Pydantic `Literal["finance"]` rejects non-finance at the edge; `DispositionWrite` model_validator forces `subjective_fit_1_5` on `acted_*` dispositions), `service.py` (server-stamps `owner_user_id` from `SETTINGS.RX_OPERATOR_UUID` — never trusts client; `_ensure_aware` coerces naive payload `created_at` to UTC; terminal-state guard on `disposition()` + `snooze()` rejects re-writes on `acted`/`dismissed` rows), `routes.py` (5 endpoints under `/v1/rx/`: POST `/recs` w/ separate `X-RX-Ingest-Token` shared secret, GET list/detail under `X-API-Key`, POST disposition/snooze). **Auth split rationale**: ingest token rotation-independent + compromised ingest token cannot read or disposition existing recs. Empty `RX_INGEST_TOKEN` → 503 (fail-loud, not silent-accept). **Frontend** lazy-loaded pages `RxFinance.tsx` (list w/ drift bar / status badge / aging+forced flags / auto-revived chip) + `RxFinanceDetail.tsx` (markdown body via react-markdown+remark-gfm, expandable JSON breakdowns, disposition panel w/ subjective-fit 1-5 picker + outcome-note textarea + snooze day-picker 1-7). Routes registered at `/rx-finance` + `/rx-finance/:id`. Hooks `useRxRecs` / `useRxRec` / `useDispositionRec` / `useSnoozeRec` invalidate both list + detail on success. **Audit-cycle protocol applied**: code-reviewer agent flagged 3 HIGH (terminal-state guard, naive-tz coercion, missing owner-UUID test assertion) + 3 MEDIUM (hardcoded RX_OPERATOR_UUID default, missing owner_user_id index, client-side forced_decision threshold dupe) + 2 LOW (index direction mismatch, toast-threshold dupe). All HIGH+MEDIUM mitigated in-line: added `forced_decision` server-computed field on `RecRead`, added `ix_recommendations_owner_user_id` to migration + ORM, added boot-time log `rx config: RX_OPERATOR_UUID=… ingest=configured|DISABLED` so misconfig is visible. 26 new tests in `test_rx_routes.py` (ingest auth 401/503, CHECK rejects fitness, list ordering, disposition fit-required gating, snooze count++, terminal-state guards, FK SET NULL on rec delete, naive-tz coercion). Full backend suite **702 pass** (was 676 pre-rx + 26 net new). Plan: `.claude/plans/rx-finance-panel.md`. **Hard rules locked**: NO Supabase reads/writes from TradingV, NO `domain != 'finance'` rows, NEVER log `RX_INGEST_TOKEN`, NO rec generation from TradingV (only `/rx-finance` slash command on laptop). New module doc: `.claude/modules/rx.md`. Pending: operator wires `RX_INGEST_TOKEN` to `.env`, runs `alembic upgrade head`, then v1.x.1-b ships hypothesis-health + position-exposure + trade-form rec linkage. Phase I unlock = real `position_thesis_match` once first trade lands w/ `related_rec_id` populated.
- **Vault-indexer Phase E (Commits 2-4): smarter retrieval — decay model + query parser + lexical/RRF** (2026-05-16): three commits on top of Commit 1's excerpt-off-bundle win, in operator-requested sequence "smarter not bigger". **Commit 2 — decay model rewrite**: replaced the exponential `weight = exp(-age_months / horizon/2)` heuristic with a ranked-grouped step ladder that matches operator's mental model (`t >> t-1 ; t-1 > t-2 ; floor after rank ≥ 4`). New `cache.vault_node.evergreen` column (idempotent ALTER), backfilled at boot via path-glob default; frontmatter `evergreen: true|false` overrides. `decay.assign_ranks(nodes, group_key)` groups by author (configurable) and orders by `published_at` desc; `decay.weight_for(node, rank=...)` short-circuits to 1.0 for evergreen / mode='off' / ungrouped, else applies `ladder[rank]` with floor for rank ≥ len(ladder). Per-domain config in `_domains.yaml`: finance `mode: ranked_grouped, ladder: [1.0, 0.6, 0.45, 0.35, 0.25], evergreen_paths: [Books/**]`; fitness + nutrition `mode: off, evergreen_paths: [**]` (operator-stated: all timeless). `search.py` materializes results → assigns ranks → applies per-result decay. Live verified: same-author Click Capital group surfaces `rk=0 → decay=1.00` for the most recent video and `rk=1 → decay=0.60` for the prior day's. **Sharp edge:** SEC filings have no `author` frontmatter → ungrouped → no decay penalty (last quarter's 10-Q does NOT dominate prior quarters). Flagged for a future refinement; Commit 3's ticker-narrowing partially addresses by tightening the candidate pool. **Commit 3 — query parser**: new `query_parse.py` extracts hard anchors from free-text queries — `tickers` (regex + lexicon gated from vault path prefixes `Filings/<T>/` and `Research/...-<t>-...`; lexicon refreshed lazily + on /reload via `search.reset_ticker_lexicon`), `kinds` (closed-set alias table mapping operator phrasing like `earnings → filing`, `transcripts → video`, `books → book + book_chapter`), and `since` (relative phrases `today / last week / this month / recent` resolved via day-offsets, plus `Month YYYY` and `YYYY` literals). `ParsedQuery.has_anchors` gates whether the SQL pre-filter actually engages — empty parse → legacy unfiltered KNN. `search.py` runs parse first; when anchors present, JOINs `vault_node n` into the KNN SQL and adds `WHERE c.path LIKE 'Filings/AAPL/%' OR c.path LIKE 'Research/%-aapl-%' AND n.kind IN (...) AND (n.published_at IS NULL OR n.published_at >= ?)`. `/search` response envelope now echoes `parsed: {tickers, kinds, since, raw_terms, has_anchors}` so callers (Claude Desktop, rx slash commands, app frontend) can introspect what the indexer detected. Live verified: query "AAPL earnings revenue guidance" → `tickers=[AAPL] kinds=[filing] raw_terms=[revenue,guidance]` → top 5 results all `Filings/AAPL/...` (was: mixed across the corpus). **Commit 4 — lexical signal + RRF hybrid**: new `lexical.py` adds an FTS5 virtual table `vault_chunk_fts` indexing `title || section || text` per chunk with `porter unicode61 remove_diacritics 1` tokenisation. `cache.init` calls `init_fts` idempotently; `app.lifespan` rebuilds when chunk count > FTS count (catches pre-Commit-4 caches); POST /reload always rebuilds (cost: ~600ms for the 1090-chunk finance corpus — bounded by the operator-tempo /reload cadence). Sanitiser uses **OR-mode** (`term1 OR term2`) not AND — operator queries are typically 3-6 concept tokens where no single chunk has all of them; AND-mode produced empty results on multi-token queries. BM25 ranking still favours chunks with more rare-token matches. `lexical.rrf_merge` fuses vector + lexical rankings by `(path, ord)` via Reciprocal Rank Fusion (`score = Σ w_i / (60 + rank_i)`); per-domain config in `_domains.yaml`: finance `vector_weight=1.0, lexical_weight=0.5` (balanced — ops queries have lexical anchors that BM25 catches well); fitness + nutrition `lexical_weight=0.05` (operator-stated heavy-vector bias because small corpora make lexical false-positives noisy). `search.py` orchestrates: vector KNN (anchor-filtered) → decay → lexical FTS → RRF merge → graph hybrid re-rank → final top-k. Lexical leg skipped on single-token queries (`LEXICAL_MIN_TOKENS=2`) and when `hypothesis_paths` constraint active (lexical can't honour the path-filter). Pure-lexical-only hits (not in top-vector-K) currently dropped from final output — they'd need a second cache.get_node round-trip for body text and the win wasn't worth it; future refinement if recall metrics show gap. **Sharp edge / fix:** apsw cursor doesn't support interleaved iteration — initial `lexical.rebuild` SELECTed rows and INSERTed FTS5 entries on the same cursor → only the first row landed. Fixed by materialising the SELECT into a list before the insert loop. Caught in test fixture; would have silently mis-populated FTS in production otherwise. **Tests added**: 14 in `test_vault_indexer_decay.py` (ladder application, evergreen bypass, off-mode, assign_ranks ordering, path-glob defaults, edge cases), 26 in `test_vault_indexer_query_parse.py` (ticker lexicon gating, kind aliases, time phrases, raw_terms after extraction, build_filter_sql, load_ticker_lexicon from mock paths), 15 in `test_vault_indexer_lexical.py` (FTS init idempotent, rebuild populate, search BM25 ordering, sanitiser strip + OR-mode, RRF merge by `(path,ord)`, weight tuning). vault_indexer suite: **81 pass** (was 25 + 55 new − 1 retired exponential-decay test, replaced with rank-based equivalent + new evergreen-bypass test). **Latency**: warm-cache `/search?excerpts=false&k=10` stays sub-second on finance — anchor pre-filter actually reduces vector candidate pool when ticker present, so AAPL-anchored queries are faster than the unfiltered baseline. **What landed in adjacent surfaces**: `_domains.yaml` has new `decay:` + `lexical:` blocks per domain (operator-curated weights); `run-dev.sh` unchanged (registry-driven config wins); kb-mcp adapter unchanged (HTTP-only contract); `/search` response shape extended with `parsed`, `decay_rank`, `decay_weight`, `evergreen`, `rrf_score`, `rank_vector`, `rank_lexical`, `lexical_score` — additive only, existing callers unaffected. **Remaining gaps surfaced**: (1) Filings ungrouped → no recency-decay among same-ticker quarterly filings (mitigated organically by ticker anchor narrowing the pool, but not a substitute for proper grouping); (2) pure-lexical-only hits dropped from final results; (3) no per-chunk FTS triggers — rebuild on /reload only (acceptable for operator-tempo /reload cadence). Plan: continued from `~/.claude/plans/now-when-i-use-radiant-yao.md` "Phase E" section.
- **Vault-indexer retrieval speedup + domain-config symmetry — Phase E (Commit 1) + Phase E.5** (2026-05-16): operator hit empty placeholder `[{path:null, score:null, text:""}]` returns from `bundle(q, scope=…)` on multi-term queries via the globally-registered `1kb_Shos` MCP. Root cause was a 25-second `_excerpt.select_top_sentences` step running per result on `/search` (BGE-encodes every sentence of every chunk × top-k); the kb-mcp adapter's 8s `timeout_seconds` fired, the exception became a single `{_domain, _error}` placeholder, and `bundle()` rendered the empty shape. **Commit 1** added an `excerpts: bool = True` query param to `tools/vault_indexer/app.py::/search` and `tools/vault_indexer/search.py::search()` — when False the per-chunk excerpt loop is skipped entirely. The kb-mcp adapter at `~/Code/kb-mcp/kb_mcp/adapters/vault_indexer.py::search()` now appends `excerpts=false` so `bundle()` callers (the MCP path) skip the dead-weight excerpt cost and get the full chunk `text` they already consume directly. UI consumers (`/v1/vault/search`, `/v1/research/ask`, frontend `VaultChunkList`) keep the default `True`, so the excerpt-sentences field they render is unchanged. Latency drop: **25s → 87-409ms** warm-cache, on the queries that previously hit the timeout; first request after restart absorbs ~4-5s BGE warm-up. **Phase E.5** then re-evaluated a side-observation that the finance `launchd` plist (`com.shourjo.kb-finance-indexer.plist`) lacked `EXCLUDE_FOLDERS` and risked leaking fitness/nutrition into the finance cache — but pre-discovery showed the cache held **0 of 372** non-finance paths because `tools/vault_indexer/config.py` already reads the `DOMAIN` env var and derives include/exclude from `<vault>/_domains.yaml` (registry shipped 2026-05-10). The original "Option C" recommendation (DOMAIN-rules-in-code) was therefore already in place; only cosmetic asymmetry remained. Phase E.5 closed it: finance plist gained `INDEXER_DB_PATH=<vault>/.indexer/cache-finance.db` and the existing `cache.db` was atomically renamed (stop launchd → `mv` `.db`+`.db-wal`+`.db-shm` → restart with new env), so the three caches are now sibling files (`cache-finance.db`, `cache-fitness.db`, `cache-nutrition.db`) with identical naming conventions. A boot-time coherence check (`config._log_config_coherence`) warns when `DOMAIN` and `INDEXER_DB_PATH` disagree: (1) DOMAIN=X + DB name ≠ `cache-X.db` ≠ legacy `cache.db` → warn (likely misconfigured plist); (2) DOMAIN unset + DB name matches `cache-<slug>.db` → warn (operator forgot DOMAIN, will fall through to legacy single-vault scope). Soft warnings only (operator may legitimately use `cache-experiment.db`). `run-dev.sh` lost the stale `EXCLUDE_FOLDERS=...` injection (registry covers it) and gained `DOMAIN=finance` + `INDEXER_DB_PATH` instead, matching plist convention exactly. **Tests:** 7 new in `test_vault_indexer_config_coherence.py` (coherent finance/fitness/legacy silent; mismatch DOMAIN×DB warns; DOMAIN-unset-but-DB-domainy warns; experiment DB warns softly), 8 new in `test_vault_indexer_domain_isolation.py` (passes_scope unit coverage for finance/fitness/nutrition/no-domain/explicit-env-override/unknown-domain/empty-registry, plus a model-dependent end-to-end ingest test that proves zero cross-pollination across two caches). vault_indexer suite: **25 pass** (was 10 + 15 new). Sharp edges: (1) renaming SQLite cache files needs the agent fully `bootout` first (not just `kickstart`) because WAL/SHM handles stay open with `kickstart`; (2) `knowledge-vault` directory is **not under git**, so `_domains.yaml` has no version control — flagged for backup, not addressed in this phase; (3) docs cascade is partial — frozen sources (`.claude/decisions/`, `.claude/reviews/`, `.claude/plans/*phase-2-vault-indexer*`) intentionally left untouched per "ADRs immutable, reviews are point-in-time" convention; current-state docs (`CLAUDE.md`, `tools/vault_indexer/README.md`, `MULTI_DOMAIN_BRIEFING.md`, `.claude/modules/vault.md`, `FINANCE_TEAM_WRITEUP.md`) all updated. Plan: `~/.claude/plans/now-when-i-use-radiant-yao.md` (final revision: 1kb_Shos-first) + this retro extends "Phase E" to include the symmetry cleanup. Phase E continues with decay rewrite, query parser, lexical+RRF hybrid (separate commits).
- **Ticker review queue — Phase D / Commit 2** (2026-05-14): closed the loop on Commit 1's `VisionResult.unknown_tickers` field — Stage 1 (Qwen2-VL) emits tickers NOT in the whitelist, but Commit 1 only logged them. Phase D wires them to a persistent operator review surface. Migration `0028_ticker_review_queue.py` adds the laptop-only table (UNIQUE on `ticker`, composite index on `(status, last_seen_at DESC)`, JSON columns capped at last 3 entries each for `channels` / `recent_video_ids` / `recent_caption_snippets`, plus `previously_dismissed_at` for the resurrection chip). New `app/ticker_review/` module: `service.enqueue_or_bump` upserts + bumps `times_seen`; `list_pending` filters `times_seen >= 2` (anti-noise: single-mention Qwen2-VL emissions stay in DB but don't surface on Today); `resolve` chains atomically to `watchlist.add_entry` / `boards.add_ticker` then flips status — destination write fails fast and keeps the queue row actionable. Re-eligibility window: dismissed rows past 90d resurrect on re-mention with `previously_dismissed_at` set so the UI renders the chip. `/v1/ticker-review/queue` + `/v1/ticker-review/{id}/resolve` endpoints registered in the v1 router. **Indexer wiring**: `youtube_channel.py:ingest_one` replaces the prior `logger.info("ticker-review pending: ...")` with `service.enqueue_or_bump_sync()` per unknown ticker; snippet derived from the matching `chart_ref.caption`; best-effort failure mode (queue write never blocks a vault draft). Sync wrapper handles both event-loop-running and fresh-thread cases. **Lifespan loop**: `_ticker_review_digest_loop` runs daily under the laptop-only branch, only emits markdown when `weekday()==6` (Sunday, NY tz) to `<VAULT_PATH>/Topics/_ticker-review-queue.md`. Sentinel-bounded (`<!-- ticker-review-queue:auto-start -->` / `:auto-end`) so operator notes outside the block survive regeneration. Registered as `ticker_review_digest` in `app/admin/loops.py` with manual-fire callable for the admin UI. **Frontend** `TickerReviewStrip.tsx` slots into Today between the 2×2 grid + `TVContextStrip`; hidden when queue empty; per-row layout: ticker + "seen N× across X, Y" + last caption snippet (italic line-clamped) + Add to roster / Add to board ▾ / Dismiss buttons + "previously dismissed YYYY-MM-DD" chip when applicable. 10-row visible cap; overflow line points operators at the vault digest. `useTickerReviewQueue` + `useResolveTickerReview` hooks invalidate `ticker-review-queue`, `watchlist`, and `boards` caches on success. **Tests**: 18 new in `tests/test_ticker_review.py` (enqueue happy path / bump / list-cap-3 / empty-ticker reject / min_seen filter / resolve add_to_roster + add_to_board + dismiss / re-eligibility window inside + outside / weekly digest markdown / HTTP queue + resolve + 404). Full suite **686 pass** (was 668 + 18 net new). Plan: `.claude/plans/ok-now-we-have-distributed-anchor.md` "Phase D" section. Sharp edges: (1) SQLite stores datetimes timezone-naive; `_ensure_aware` coerces on read before the re-eligibility subtraction (Postgres preserves tz so the same code path is a no-op in prod). (2) Chain-before-status-update ordering matters — a `boards.add_ticker` LookupError on a missing board_id leaves the queue row `pending`, not silently marked `added_to_board`. (3) Laptop-only state: no `sync_outbox` row on writes; operator-instance decision per the plan. The Sunday digest is the only artifact that crosses into the shared vault. New module doc: `.claude/modules/ticker_review.md`.
- **Structured chart references + channel vignette auto-enrichment** (2026-05-14): operator wanted L3 captions converted into **structured, retrievable signal** so the inference layer can answer queries like "what 4H BTC candle references happened in past 90 days" without LLM-parsing prose at retrieval time. Three-stage extraction chain in `tools/vault_indexer/ingest/`: **Stage 1** `vlm_adapter.caption_frame_structured` runs Qwen2-VL with a YAML/JSON prompt for `chart_type` + `timeframe` + `tickers` + `topics` + `caption`. **Stage 2** `chart_extractor.extract_from_caption` regex-salvages chart-type + timeframe + tickers from the prose when Stage 1 emits empty signal. **Stage 3** `vlm_adapter._regex_salvage` is field-by-field last-resort extraction from raw output when YAML/JSON parse fails (Qwen2-VL frequently emits JSON-fenced-as-YAML with hallucinated repeats; field-by-field salvage recovers ~100% of frames). Ticker whitelist: dynamic union of `roster ∪ all boards ∪ The Street tier-1/2 last 4 snapshots`, loaded at start of each ingest tick. Static stoplist (`AI`, `USA`, `GDP`, etc.) filters acronym noise even with empty whitelist. **Channel `_index.md` auto-enrichment** via `vignette_updater.py`: sentinel-bounded auto-section (`<!-- AUTO:chart-references:start --> ... <!-- AUTO:chart-references:end -->`) keeps a rolling table of recent chart references per channel, FIFO-evicted at `rollup_cap` (default 10), dedupe-keyed on `rel_path` (the markdown link target). Operator-authored content above and below the markers is preserved on every upsert. **Video-level summary block** inserted above the Visual Notes table by `render_draft` when chart_references present: `**Charts referenced (5):** 2× line, 1× bar; **Timeframes:** 4h, 1d; **Tickers:** AAPL, MSFT, AMZN; **Topics:** ...`. **Per-channel toggle** `vision.chart_extraction.enabled` default `false` — fitness/nutrition channels pay zero cost. **fx-evolution-daily + click-capital opted in**. Stage 1 ticker hits NOT in whitelist captured on `VisionResult.unknown_tickers` (currently logged-only; wired to ticker-review queue in next-session Commit 2). **Pilot results** (click-capital, frame_budget=5, both toggles on): 5/5 frames produced structured `chart_references` with chart_type + timeframe + ticker triples; captions clean (no raw YAML leakage in the rendered table). New tests: 13 chart_extractor + 14 vignette_updater + 6 new video_vision (chart_extraction wiring) = 33 net new. Full suite **668 pass** (was 612). Plan: `.claude/plans/ok-now-we-have-distributed-anchor.md` "Structured chart references" + "Phase D" sections (D deferred to Commit 2). Sharp edges: (1) Qwen2-VL structured output stability is shakier than free-form — regex salvage is mandatory, not optional; (2) `app.watchlist.service.list_symbols` + `app.boards.service.list_boards` require the FastAPI `Settings` env (DATABASE_URL + API_KEY) — when CLI runs the whitelist loader without these, watchlist/boards silently degrade to empty and The Street alone fills the universe (~29 entries observed); not a regression for the channel poller which runs under the FastAPI lifespan.
- **MLX acceleration — Whisper adapter + L3 semantic captions** (2026-05-14): operator's L2 pilot answered "captures source attributions + on-screen text" but couldn't answer "what chart TYPE" or "what TIMEFRAME on the candle chart" because Tesseract OCR only reads text. Operator approved L3 (local VLM) but only via MLX (Apple Silicon GPU) — CPU was too slow (~2-3s/frame). Same MLX rationale extended to existing Whisper transcription (audio path was openai-whisper on torch CPU, ~3-5× slower than MLX-Whisper on M3). Adapter pattern keeps tests + non-Apple-Silicon environments working. **Phase 1 — Whisper adapter** (`tools/vault_indexer/ingest/whisper_adapter.py`): `_mlx_available()` checks Darwin+arm64+importable `mlx_whisper`; `FORCE_TORCH_WHISPER=1` env override; `transcribe()` and `transcribe_with_segments()` route to MLX or torch; failure returns `""` (never raises). Both call sites (`ingest_video.py:transcribe`, `youtube_channel.py:whisper_transcribe`) updated to use the adapter — signatures unchanged so existing test mocks at line 444 still hit. **Phase 2 — VLM adapter** (`tools/vault_indexer/ingest/vlm_adapter.py`): module-level singleton model cache (loaded once per process, ~10-15s warm-up); `caption_frame()` calls `mlx_vlm.generate` for a single frame; boilerplate stripper (`"The video frame shows X" → "X"`); `DISABLE_MLX_VLM=1` env override. Originally pinned `Moondream2` but `mlx-vlm 0.5.x` doesn't include moondream2 weights — pivoted to `mlx-community/Qwen2-VL-2B-Instruct-4bit` (~1GB disk, 2B params, 4-bit quantised). Pilot validated: Qwen2-VL correctly identifies "candlestick chart type, displaying a timeframe of 4H", "line chart, focus on the peak in March 2000", Fear & Greed gauge readings — directly answers operator's earlier "what chart type + what timeframe" questions. **Phase 3 — `video_vision.py` L3 wiring**: `render_visual_notes_table()` accepts optional `captions=` kwarg → 3-column table (Time / Visual / On-screen text); absent → byte-identical to L2 2-column output (regression-tested). `process_video()` branches on `cfg.semantic_captions`; gracefully degrades to L2 when mlx-vlm unavailable. CLI gained `--semantic-captions` flag for pilot. **Sharp edge:** thread-based per-frame timeout removed — MLX maintains a thread-local GPU stream so `ThreadPoolExecutor`-based timeouts fail with "There is no Stream(gpu, 1) in current thread" on second-and-later calls. Frame-budget cap at the orchestrator is the real cost bound. **Tests:** 12 new whisper-adapter tests + 11 new vlm-adapter tests + 8 new video_vision tests (3-column path, L3 enable/disable, fallback when VLM unavailable). Full suite **612 pass** (was 573 + 31 new). **Pilot:** ran against `https://www.youtube.com/watch?v=TWeitsa7f4s` (click-capital) with budget=6, semantic_captions=on → 6/6 frames captioned in ~30s wall (including video DL + scene detect + OCR + caption). One-time Qwen2-VL download ~2.5min. **Channels opted in:** fx-evolution-daily + click-capital `_channel.yaml`s have `semantic_captions: true`. All other channels untouched (default false). **Disk added:** ~1GB (Qwen2-VL-2B-4bit). New deps: `mlx-whisper`, `mlx-vlm`. Plan: `.claude/plans/ok-now-we-have-distributed-anchor.md` "MLX migration" section. Module docs: `.claude/modules/video_vision.md` extended with L3 + adapter sections.
- **Local video-vision (L2 — Scene + OCR) for ingested videos** (2026-05-14): operator wanted to capture on-screen content the audio transcript misses (tickers, source attributions, periodic windows, on-screen polls, chart titles) for fx-evolution-daily and click-capital. Constraint: no API spend, no image persistence, must work across any future channel / domain (fitness, nutrition, etc.). Built `tools/vault_indexer/ingest/video_vision.py` — three-stage scene-cut + Tesseract OCR pipeline. Stage 1: ffmpeg `select=gt(scene,0.10)` — 10% pixel-change threshold rejects cursor wiggles (~0.0005%) while catching ticker swaps on the same chart layout (~30-50%). Stage 2: 10s minimum-gap dedupe collapses rapid succession (presenters hold a chart ≥10s for the audience). Stage 3 (optional): SSIM > 0.92 redundancy filter. Stage 4: budget cap (default 50 frames, top scene-score in budget, restore time order). All four stages tunable per-channel via a `vision:` block on `_channel.yaml`. Pilot results: click-capital extracted **23 useful rows from 30 raw frames** — Business Insider/Barron's/FactSet attributions, Paul Tudor Jones + Michael Burry quotes, "S&P 500 Drawdowns Under New Fed Chairs", "Bloomberg Commodity Index", AAII poll — exactly the micro-references operator wanted. fx-evolution short talking-head yielded 3 useful rows. Wired into `youtube_channel.py:ingest_one()` — `process_video()` called inside the existing `TemporaryDirectory()` scope so frames and downloaded video are deleted on context exit (no image persistence past the run, verified). `render_draft()` gained optional `visual_notes_md` + `visual_notes_frame_count` kwargs; absent → zero behaviour change. **Multi-domain reuse confirmed:** same module, same code path, same toggle works for fitness/nutrition/any future indexer scope — drop a `vision:` block on any `_channel.yaml`. Defaults locked: `scene_threshold: 0.10` (operator-set, replaced earlier 0.4 draft), `min_gap_seconds: 10`, `ssim_threshold: 0.92`, `frame_budget: 50`, `ocr_lang: eng`, `semantic_captions: false` (L3 Moondream2 deferred). Storage footprint: Tesseract system binary ~50MB (smaller than existing Whisper-small at 484MB); zero ongoing image/video persistence; markdown additions ~1-5 KB per video. New module doc `.claude/modules/video_vision.md` (formula + knob math table + multi-domain reuse + storage story + kill switch + L3 upgrade trigger). `pytesseract` added to `requirements.txt` with `brew install tesseract` prerequisite noted. 33 new tests in `tests/test_video_vision.py` (parse, dedupe, budget, threshold, OCR noise filter, render, config, process_video graceful degradation). Full suite **573 pass** (was 540 + 33 new − 1 pre-existing TZ-flaky earnings test unrelated to this work). `vision: {enabled: true, ...}` blocks added to fx-evolution-daily + click-capital `_channel.yaml`s. All other channels untouched. Plan: [`plans/ok-now-we-have-distributed-anchor.md`](../plans/ok-now-we-have-distributed-anchor.md) "Local video-vision REVISED" + "EXECUTION PLAN" sections.
- **Today landing redesign + research-queue ranking** (2026-05-13): operator's pre-existing pain — 10+ pending research approvals stacked above the fold with inline buttons → cognitive overload → blanket dismissal → signal lost. Brainstormed root cause as volume × placement × ranking; converged on demo-inspired 2×2 narrative grid + bottom-of-page pending panel capped at top-5 by composite score (not chronology). Phase 1 backend: migration `0027_research_query_scoring.py` adds `score FLOAT`, `is_deferred BOOLEAN`, `auto_aged_at TIMESTAMPTZ` to `research_queries` + composite index on `(status, is_deferred, score DESC)`. New `app/research/ranking.py` module exposes `compute_score(query)` (composite of has_action + at_risk_hyp + recent_at_risk_eval + log(cost) − dismiss_rate − age_days, all tunable constants at module top), `recompute_all_pending(session)` (nightly via retention loop, rebalances top-5 visible vs deferred backlog), and `auto_age_expired(session, threshold_days=30)` (auto-dismisses stale pending with `approved_action={"reason":"auto-aged-out"}`). Service `ask()` computes initial score on persist (best-effort, doesn't block query creation). `/v1/research/queries` gained `?order={asked_at,score}` (default `asked_at` for legacy callers) + `?include_deferred=` (default true; landing uses false). Retention loop now calls auto_age + recompute_all_pending in addition to existing status-based sweeps. `ResearchQueryRead` schema gains `score`/`is_deferred`/`auto_aged_at` fields. 13 new tests (score components, at-risk boost, action boost, age penalty, dismiss-rate penalty, recompute top-5 split, auto-age 30d, idempotent age, endpoint order, endpoint deferred filter). Full suite **540 pass** (was 528 + 13 new − 1 pre-existing TZ-flaky earnings test unrelated to this work). Phase 2 frontend: 4 narrative cards (`DriftCard`, `FreshSignalsCard`, `ResearchCuriousCard`, `MarketMoodCard`) replace the legacy strip-stack — each carries a role description that survives empty state, no inline action buttons. New `PendingReviewPanel` at bottom of Today: top-5 rows collapsed by default, click expands inline with Approve/Dismiss buttons (no modal hop), footer link to `/research?status=pending` for backlog. Operator-locked decisions: cap=5, server-side scoring, 30d auto-age, 2×2 symmetric grid, NO inline actions on the landing. `useResearchQueries` hook gained `order` + `includeDeferred` params; `ResearchQueryRead` TS interface mirrored the backend schema additions. Frontend `tsc --noEmit` clean. Plan: [`plans/today-redesign-and-research-ranking.md`](../plans/today-redesign-and-research-ranking.md). Sharp edges: `ResearchApprovalStrip` component file kept around (still useful on `/research`) but no longer imported by Today; nightly recompute runs in retention loop so new queries don't appear deferred until next sweep — acceptable since initial score is computed at create time and a brand-new query is unlikely to need immediate demotion.
- **Demo branch claim audit + trim** (2026-05-12): public demo on Cloudflare Pages was making claims main app couldn't back. Five hard overpromises (fictional rule names `trend-breakout`/`mean-reversion`/`gap-fill`/`momentum-thrust`; specific failure-mode numbers `~12pt` VIX-regime / `~2× MAPE` earnings-window / `~6pt` mega-cap-exclusion; "Request access" CTA implying productized signup) plus six soft overpromises (research-weekly implied automatic when it's opt-in; placeholder walkthrough videos shown as primary CTA; unqualified "posts to Telegram"; TV webhook conflated with screenshot paste path; closed-loop CTA implied auto-trade-logging) and three benchmark-drift items audited. Pre-implementation stress-test surfaced bake-script-is-skeleton problem (demo data is hand-placed, not DB-driven — affects every numeric refresh), cross-repo coupling invariant (`tools/vault_indexer/` shared by knowledge-vault + custom MCPs, never touched by these remedies — confirmed safe), and persuasion-vs-honesty trade-off (Phase 5 of demo deliberately added specific numbers per B2B-demo research, trimming reverts to generic-claim mode). Chose trim-copy path: 10 files touched on demo branch — `canned.json` + `motion/{opportunities,trades}.json` + `today.json` + `AskWidget` + `DemoBanner` + `WatchWalkthrough` (now hides button entirely when no `youtubeId` and `VITE_SHOW_WALKTHROUGH_PLACEHOLDERS!=='true'`) + `DemoAbout` (4 failure-mode blocks rewritten to match shipped capability) + `DemoHome` (curious-about card disclaims opt-in) + `DemoMotion` (closed-loop subhead discloses manual logging). Fictional rule slugs remapped to real R1/R2/R3 labels (`BUY +2% over 5d (HR≥60%)` / `SELL -2% over 5d (HR≥60%)` / `BUY +5% over 10d (HR≥55%)`) preserving per-rule attribution diversity; horizon fields aligned. Demo `tsc --noEmit` clean. Pushed `3494178..5ba9570 demo -> demo`; CF Pages auto-rebuild triggered. No main-app touch. Plan: [`plans/ok-now-we-have-distributed-anchor.md`](../../.claude/plans/ok-now-we-have-distributed-anchor.md) "Demo Branch Claims Audit" section. **Deferred (filed to backlog):** bake-script-functionalisation (~1d), three optional partial-wire ship remedies (earnings-window prediction tagging, cap-size cohort filter on `/accuracy`, drift `regime_at_alert` annotation) — all retain operator-utility independent of demo, decision deferred.
- **Cost-aware iteration Phase 5 — Retention + costs dashboard** (2026-05-09): closes the iteration. New `app/admin/costs.py` (monthly breakdown / 30-day daily series / top queries with 5-min in-process cache) and `app/admin/retention.py` (per-class sweep functions + status listing). New vault helpers: `tools/vault_indexer/cleanup_filings.py:cleanup_old_8k()` (drops 8-K filings older than 18mo, keeps 10-Q/10-K forever) and `tools/the_street/consolidate.py:maybe_rollup_quarter()` (writes `<vault>/The Street/quarterlies/<YYYY-QN>/quarter-rollup.md` BEFORE deleting source weekly dirs — keeps last 13 raw snapshots). New `_retention_loop` in lifespan, gated `if not is_railway`, daily; manual fire via `/v1/admin/loops/retention/fire`. **Per-class retention matrix locked**: `prediction_accuracy` 365d (`evaluated_at`), `drift_alerts` 90d (acked only — unacked stay forever), `research_queries` per-status (approved + pending forever, dismissed 180d, error 90d), 8-K filings 18 months. Six new admin endpoints: `/v1/admin/costs/{monthly,recent,top-queries}` + `/v1/admin/retention` + `/v1/admin/retention/{key}/purge` (two-step preview→confirm, capped 5000 rows/click). `app_settings` allowed-prefix list extended to include `retention.*`. Frontend: `useCostsMonthly` / `useCostsRecent` / `useRetentionStatus` / `usePurgeRetention` hooks; new `CostsTab.tsx` (monthly totals + cap progress bar w/ color states + 30-day stacked mini-bar chart + Vision-monthly toggle + kill-switch banner) and `RetentionTab.tsx` (per-class table with row counts + oldest row + per-status sub-counts for research_queries + two-step purge button). 12 new tests (3 retention sweeps + 3 endpoint tests + 3 cost-aggregator tests + 3 The Street consolidate tests). Full suite **528 pass** (was 516). Frontend TS clean. Plan: [`plans/ok-now-we-have-distributed-anchor.md`](../../.claude/plans/ok-now-we-have-distributed-anchor.md). Sharp edge: PredictionAccuracy has FK chain (→ prediction_points → analysis_tasks → analysis_jobs); test coverage of that sweep simplified to empty-path assertion since all three sweeps share identical SQL pattern. Phase 4 of the original IA-reorg plan (Gekko auto-pipeline) **continues to be deferred** — most of what it covers is now redundant with EDGAR direct ingest.
- **Cost-aware iteration Phase 2 — Earnings calendar + IR-channel trigger polling** (2026-05-09): new `app/earnings/` module + `earnings_calendar` table (migration `0026_earnings_calendar.py`). `compute_universe()` returns roster ∪ The Street Tier 1+2 over the last 4 snapshots, capped at 150 with a 90-day TTL after last appearance. Provider chain: yfinance (primary) → NASDAQ JSON (fallback) → EDGAR 8-K Item 2.02 confirm (US-only; non-US tickers like TSM/BABA skip). Stale-date sanity treats anything > 7d before today as a miss. Tiered cadence: skip the lookup when `fetched_at` is within 7 days AND `expected_at` is more than 14 days out. Lifespan loop `_earnings_calendar_loop` registered with the admin runtime; cadence editable via `/admin/cadences`; manual fire via `/v1/admin/loops/earnings_calendar/fire`. Three read endpoints: `/v1/earnings/{upcoming,{ticker}}` + a 180-day list. **IR YouTube poller modded**: `_channel.yaml` gains optional `earnings_trigger: { tickers, days_before, days_after }` block; `youtube_channel.ingest_one()` short-circuits with `reason: 'earnings_trigger_gate_closed'` outside the window — saves Whisper CPU on non-earnings days. Multi-ticker support (Alphabet IR for GOOGL + GOOG fires on either). NY-tz date math. Lifespan threads `earnings_dates` from `upcoming_earnings(60)` into `ingest_all`. `seed_ir_channel.py` writes the trigger block by default + `prefer_captions: 'manual_only'` (long-form earnings calls have flaky auto-captions). 18 new tests (universe/cap/provider chain/stale-date/purge/trigger window/multi-ticker/poller gate). Full suite **516 pass** (was 498). Frontend TS clean. **Outbox sync to Railway intentionally deferred** — laptop is the read source for v1 Today panel. Plan: [`plans/ok-now-we-have-distributed-anchor.md`](../../.claude/plans/ok-now-we-have-distributed-anchor.md). Sharp edge: existing `app/main.py:_edgar_ingest_loop` had a stale `WatchlistItem` import (real class is `WatchlistEntry`) — fixed in passing.
- **Cost-aware iteration Phase 4 — Process status + manual fire/abort + cost guards** (2026-05-09): **biggest** of the five phases. New `app/admin/` module shipped with three files (`loops.py` static registry, `runtime.py` in-process handle store, `service.py` settings cascade + `record_tick` + cost guards), one lifespan helper (`lifespan.py` with `tick_status` async-context-manager + `register_handle` + `assert_registry_drift`), and the routes module (`/v1/admin/loops` + `/{id}/{fire,abort,cadence}` + `/v1/admin/settings`). Two new tables (`app_settings`, `process_status`) via migration `0025_admin_settings_and_process_status.py` — both per-instance, neither replicates via the sync outbox. **Settings cascade locked**: DB > env > hardcoded default. `get_setting(key)` reads on every tick so operator edits propagate without restart. **Cost guards C1–C6 implemented**: research_weekly default OFF; cap default $5/mo with auto-flip kill-switch at 100%; vision-month toggle independent of master kill-switch; manual fire is the primary path for everything Anthropic-priced. `app/research/service.py:ask` and `app/tv_context/vision.py:summarize_chart` short-circuit to synthetic responses (preserving the normal shape) when `anthropic_kill_switch_active()` returns true. **Lifespan integration**: 13 loops registered with handles (some with `fire_now` callables, all with `stop_event`/`task` for abort). 30s server-side debounce on fire returns 429 with `retry_after_seconds`. Drift check at startup logs unregistered handles. Frontend wired: `useAdminLoops`/`useFireLoop`/`useAbortLoop`/`useUpdateCadence`/`useAdminSettings`/`useUpdateAdminSetting` hooks; `ProcessRow.tsx` with status icon + relative-time + confirm-modal-on-fire for sensitive loops; `ProcessesTab` table with auto-refresh; `CadencesTab` with per-loop editor + Anthropic kill-switch toggle. **20 new tests** (8 settings cascade + 12 routes), full suite **498 pass** (was 478). Frontend TS clean. Plan: [`plans/ok-now-we-have-distributed-anchor.md`](../../.claude/plans/ok-now-we-have-distributed-anchor.md) "Handover" section. Phase 2 (earnings calendar) is next; its migration was renumbered to 0026 since 0025 is now this phase's.
- **Cost-aware iteration Phase 3 — Admin shell with tabs** (2026-05-09): consolidated scattered admin surfaces into a single tabbed `/admin/:tab?` page mirroring `/predictions/:tab?` + `/macro/:tab?`. Six tabs: **Processes** (default) / **Cadences** / **Costs** / **Retention** / **Schedule** / **Jobs**. Each tab is conditionally rendered (no preemptive `useQuery` outside the active tab) and wrapped in a `TabErrorBoundary` so one broken tab doesn't blank the page. Schedule + Jobs render the existing pages unchanged via thin `SchedulePanel.tsx` + `JobsPanel.tsx` wrappers (`JobsPanel` switches to `AnalysisJobDetail` when `/admin/jobs/:jobId` matches). Processes/Cadences/Costs/Retention start as placeholder stubs — Phase 4 fills Processes + Cadences with the loop registry; Phase 5 fills Costs + Retention. Sidebar Admin group simplified to `[Admin (tabbed shell), Legacy Dashboard]`. New `exact?: boolean` flag on `NavLeaf` so Legacy Dashboard at `/admin/overview` doesn't accidentally also highlight the parent Admin entry. Legacy redirects: `/schedule` → `/admin/schedule`, `/health` → `/admin/jobs`, `/health/:jobId` → `/admin/jobs/:jobId`, `/analysis/:jobId` → `/admin/jobs/:jobId`. Frontend-only — no backend touch. Admin route lazy-loaded. TS clean. Plan: [`plans/ok-now-we-have-distributed-anchor.md`](../../.claude/plans/ok-now-we-have-distributed-anchor.md) "Handover" section.
- **Cost-aware iteration Phase 1 — Skill picker on Research page** (2026-05-09): wired the operator-selectable skill picker on `/research` to the already-shipped `GET /v1/research/skills` endpoint. New `SkillPicker.tsx` renders segmented chips on desktop and switches to a native `<select>` on mobile when more than 4 skills are available; persists the chosen slug to `localStorage('research.last_skill_slug')` and falls back to the `default: true` skill on first visit. Verdict-only sub-label appears when the active skill exposes `tool: null`. `AskRequest.skill_slug` plumbed from picker → `useResearchAsk` → backend service (already accepts the field). New `useResearchSkills()` hook (5-min staleTime). Empty list fallback hides the picker entirely. Frontend-only — no backend touch. TS clean. Plan: [`plans/ok-now-we-have-distributed-anchor.md`](../../.claude/plans/ok-now-we-have-distributed-anchor.md) "Handover" section.
- **Free-tier adoption of patterns from anthropic/financial-services** (2026-05-09): five-item follow-on plan from the external review (`.claude/reviews/2026-05-09-external-anthropic-financial-services.md`) shipped end-to-end after operator constraint update ("avoid paid services"). Item 1 — **Skills factoring**: research stress-test prompt extracted into operator-editable `skills/research/<slug>.md` with YAML frontmatter + sectioned body (methodology / example bundle / example query / example verdict / example tool call). New `app/research/skills.py` parses + caches by mtime so operator edits propagate without restart. Two new skills authored beyond the extracted stress-test: `research-comp-scan` (peer-set comparison, verdict-only) and `research-earnings-followup` (post-earnings delta, verdict-only). `/v1/research/ask` gained optional `skill_slug` param + new `GET /v1/research/skills` endpoint for the frontend. Backwards-compat preserved: when no skill matches, falls back to legacy hard-coded prompt. Item 2 — **SEC EDGAR direct ingest**: new `tools/vault_indexer/ingest/ingest_edgar.py` with ticker→CIK lookup (cached weekly under `<vault>/.indexer/edgar_company_tickers.json`), Atom feed parse, polite User-Agent enforcement (SEC requires `EDGAR_USER_AGENT`), self-throttle to 5 req/s, primary-doc URL discovery (skips SEC's `*-index.htm` metadata pages), readability-lxml text extraction, idempotent vault writes (matched on `accession_number`). Form types 8-K / 10-Q / 10-K supported in v1; Form 4 + 13F-HR deferred (different parsing). New gated lifespan loop `_edgar_ingest_loop` in `app/main.py`, opt-in via `EDGAR_INGEST_ENABLED=true`, polls every roster ticker every 6h. 14 unit tests with mocked HTTP. Item 3 — **IR YouTube channel seeder**: rather than pre-fill 12 channels with possibly-wrong handles, shipped `tools/vault_indexer/ingest/seed_ir_channel.py` which resolves a `@handle` to a UC… channel ID and creates `<vault>/Videos/earnings-<ticker>/_channel.yaml` with sensible defaults (weekly cadence, 3-month horizon, `[earnings, <TICKER>]` tags). Operator-facing `<vault>/Videos/_README.md` documents the pattern + lists candidate IR handles to verify before seeding. Item 4 — **RSS feed mode for ingest_newsletter**: extended `tools/vault_indexer/ingest/ingest_newsletter.py` with namespace-agnostic feed parser supporting both RSS 2.0 and Atom, idempotent on entry GUID (frontmatter `source_guid`), per-entry full-article fetch via existing readability path with feed-summary fallback. `--feed <url> --author <slug> --tag x --max-entries N` CLI mode. Operator can now point at Reuters RSS, Fed press releases, Treasury, SEC, etc. without code change. Item 5 — **Steering events in research weekly loop**: hypotheses pre-ranked by at-risk status (TTL ≤ 30d) and days-to-expire so urgent ones stress first. Each tick appends a structured event log to `<vault>/Research/_steering-log.md` (markdown table + fenced JSON for tooling); diagnostic file starts with `_` so the indexer skips it. Handles tz-naive Postgres TIMESTAMPTZ correctly. **Decision dropped from active list:** MCP-pluggable provider seam (originally Tier 2 in the external review; YAGNI without a second provider in flight; reactivation trigger is yfinance-the-library breaking, not per-symbol 404s). Plan: [`plans/ok-now-we-have-distributed-anchor.md`](../../.claude/plans/ok-now-we-have-distributed-anchor.md) "Follow-on Plan" section. Total: 478 tests pass (was 448), zero regression.
- **IA reorg Phase 3 — Theses page + consolidated Watchlist** (2026-05-08): hypothesis surface promoted from sidebar-only widget to a first-class page at `/theses`. Two-column layout: filterable status chips (active/expired/invalidated/cancelled/manual_closed/all) + selectable list + detail panel rendering `body_md` markdown and `recent_evaluations` rows. Two inline action buttons: "Stress this thesis" posts to `/v1/research/ask` with `hypothesis_slugs=[slug]` and navigates the operator to `/research?id=<query_id>` on success; "Cancel" opens an inline reason input then `POST /v1/hypotheses/{id}/cancel`. Deep-link via `/theses?id=<hypothesis_id>`. New components under `frontend/src/components/theses/{ThesesList,ThesisDetail,EvaluationRow}.tsx`. **Watchlist consolidation:** the old `/roster` (operational watchlist driving Kronos run) and `/watchlists` (casual ticker boards) collapse into a single `/watchlist` surface (`WatchlistConsolidated.tsx`) — Roster at top, Boards below an `<hr/>` with `#boards` anchor. Both backend endpoints (`/v1/watchlist`, `/v1/boards`) remain separate; only the frontend mental model merges. Redirects: `/roster` → `/watchlist`, `/watchlists` → `/watchlist`, `/watchlists/:boardId` → `/watchlist#boards`. Theses + Watchlist added to nav (Theses under Think, Watchlist under Decide). Admin group cleaned (Roster removed). No backend touch this phase. Frontend TS clean. Plan: [`plans/ok-now-we-have-distributed-anchor.md`](../../.claude/plans/ok-now-we-have-distributed-anchor.md). **Phase 4 (Gekko auto-pipeline) remains future work** — config seam already in place at `<vault>/The Street/_source.yaml`.
- **IA reorg Phase 2 — Ticker Hub + The Street UI + vault proxy + Today expansion** (2026-05-08): the Signal→Ticker→Thesis funnel now has a join page. New `/ticker/:symbol` hub aggregates predictions (h+1 accuracy pair), per-symbol smart-money snapshots, recent TV Context, hypotheses touching the symbol (client-side filter against active list — Phase 3 will add a server-side filter when `Hypothesis.tickers` becomes a column), vault chunks (semantic search), open opportunities, and recent trades. New `/the-street` page surfaces the smart-money universe in three modes: latest Tier 1/2/3 tables, per-ticker timeline, and snapshot browser that renders the snapshot's `_index.md` via the new vault-indexer node proxy. New backend modules: `app/the_street/` (thin async wrappers around `tools/the_street/query.py` — `/v1/the-street/{snapshots,ticker/{sym},tier/{1\|2\|3},politician/{name}}`) and `app/vault/` (httpx forwarder for indexer `/search`, `/folder-context`, `/node/{path}` — keeps port 8001 out of the frontend). Today expanded with TVContextStrip (recent active items across roster top-8, ring on items captured since `localStorage('today.last_visited_at')`) + WatchlistDelta (added/removed vs last snapshot). New `<TickerLink symbol={...}>` helper deep-links every ticker mention to the hub; wired on Opportunities + Trades pages this phase (more pages to migrate as touched). 25 new backend tests (15 the_street + 10 vault_proxy) all green. Frontend TS clean. Plan: [`plans/ok-now-we-have-distributed-anchor.md`](../../.claude/plans/ok-now-we-have-distributed-anchor.md).
- **IA reorg Phase 1 — Today page + sidebar regroup** (2026-05-08): operator confirmed two pain points — intelligence layer feels disconnected from decision surface, and 13 frontend routes of unequal daily importance share equal sidebar real estate. Phase 1 ships frontend-only nav-and-home reshape: new `Today.tsx` page replaces generic `Dashboard.tsx` at `/`. Today renders DriftBanner (hidden when no alerts) + ResearchApprovalStrip (pending queries with inline Approve/Dismiss + Detail link, status filter `'pending'` per `ResearchQueryRead.status`) + FreshSignalsStrip (sparkle-flagged opportunities arriving since `localStorage('today.last_visited_at')`) + collapsed RegimeStrip. Sidebar `NAV_GROUPS` regrouped by user-job: **Today** (leaf) / **Decide** (Signals→Predictions→Macro→Watchlist) / **Think** (Research→TV Context→The Street) / **Admin** (Roster/Schedule/Health/Legacy Dashboard, collapse-default via `group.id === 'admin'` branch in `NavSection`) / **Docs**. Legacy Dashboard kept at `/admin/overview` for unchanged behaviour. New components under `frontend/src/components/today/{DriftBanner,ResearchApprovalStrip,FreshSignalsStrip}.tsx`. No backend touch. TS clean (`npx tsc --noEmit`). Phase 2 will add Ticker Hub at `/ticker/:symbol` + The Street UI at `/the-street` + thin `app/the_street` + `app/vault` route proxies. Plan: [`plans/ok-now-we-have-distributed-anchor.md`](../../.claude/plans/ok-now-we-have-distributed-anchor.md).
- **The Street — smart-money snapshot vault folder + read CLI** (2026-05-08): packaged 2026-05-08 Gekko aggregate (4 Tier-1 META/TSM/MU/GOOGL, 25 Tier-2, 8 Tier-3) into a new vault top-level folder `~/Documents/knowledge-vault/The Street/` with `_index.md` operator vignette, `_schema.md` writeup contract, `_README.md` runbook, `_source.yaml` periodic-update config stub (gekko / SEC EDGAR Form 4 / House STOCK Act / Senate eFD / 13F-HR / options flow — all sources `enabled:false` until pipeline ships). Snapshot writeups: `tier-1/2/3-conviction.md`, `politicians-buys.md`, `insiders-top.md`, `options-bullish.md`, `methodology.md`, `_index.md`. Raw TSV/JSON copied verbatim under `data/2026-05-08/` for audit. Read-side CLI `tools/the_street/query.py` exposes `--ticker / --tier / --politician / --list-snapshots` against the aggregated TSV; library functions `find_ticker / list_tier / find_politician / latest_snapshot`. Smoke-test green: Tier-1 lists 4 expected names, Cleo Fields → 3 disclosures (META/GOOGL/MU). Vault taxonomy gained 11 tags (`smart_money`, `institutional_flow`, `insider_activity`, `political_disclosure`, `options_flow`, `cross_channel_conviction`, `tier1/2/3`, `methodology`). `.claude/vault.md` cross-link added. Periodic Gekko auto-update remains FUTURE work (Phase 4 of the IA-reorg plan).
- **YouTube channel poller — Shorts filter + auto_promote wiring** (2026-05-08): operator's `_channel.yaml` had `auto_promote: true` set but the poller never read it — drafts piled up as `.md.draft` invisible to Obsidian (which only shows `.md` by default). Three RSS entries this week were YouTube Shorts (URLs containing `/shorts/`) which operator wanted excluded entirely. Wiring: extracted `promote_draft_path()` helper from `review.promote()`, then in `youtube_channel.ingest_one()` partition feed entries into Shorts (skipped + marked seen so RSS re-surfacing is a no-op) vs long-form (transcribed + drafted). After the draft loop, if `cfg.ingest.auto_promote` is True, walk just-written drafts and rename via the helper. New `tools/vault_indexer/cleanup_shorts.py` CLI: walks `<vault>/Videos/**/*.{md,md.draft}`, deletes files whose frontmatter `source_url` contains `/shorts/`. Idempotent; doesn't touch `_channel.yaml` (deleted video_ids stay in `seen_video_ids` to prevent re-fetch). Ran once: 3 Shorts deleted, 2 long-form drafts promoted to `.md`. Vault now clean. +4 unit tests in `tests/test_youtube_channel.py` (`_is_short`, ingest skips shorts, auto_promote on/off paths). Full suite **394 pass** (+27 since previous tally — many from concurrent test additions across the session). Docs: `.claude/vault.md` operator-edit-flows section now documents `auto_promote` semantics + the cleanup CLI; `use_me_guide.md` has a full channel-poller section.
- **Video ASR cleanup — Canary-Qwen 2.5B fully removed from disk + venv + code** (2026-05-06, same-day): after spike showed Canary unusable on 16GB M3 (memory thrash + degenerate TTS output, real-speech unverified), operator decided "no dead space." Removed 4.8GB HF model cache, uninstalled `nemo_toolkit` + 14 NeMo-induced packages from venv (~1.1GB), stripped `transcribe_canary()` + ffmpeg-chunking helpers + `--asr` flag from `tools/vault_indexer/ingest/ingest_video.py`. Restored `numba` after discovery that Whisper depends on it for word-timestamps. Final state: ingest_video.py is back to a single Whisper code path with a richer `--whisper-model` choice list (tiny/base/small/medium/large/large-v3). Docs synchronised: `use_me_guide.md`, `.claude/vault.md`, `.claude/tech_debt.md` all reflect Whisper-only with the trigger to revisit Canary documented. 367 tests still green. Net disk reclaimed: ~6GB.
- **Video ASR refactor — Canary-Qwen 2.5B opt-in path; Whisper stays default** (2026-05-06): planned to swap default video transcription to NVIDIA Canary-Qwen 2.5B (top of HF open-ASR leaderboard, English-only, LLM-fused decoder, claimed better numbers/jargon accuracy than Whisper-large-v3). Spike on M3 16GB revealed two blockers: (a) ~5GB model + ~7-10 min cold load + heavy memory pressure caused thrashing alongside normal operator workload (Claude Helper, Chrome, Docker, dev server); (b) initial test on synthetic TTS audio produced degenerate output (3 tokens "Transcript"+EOS) and we couldn't re-test on real speech because the follow-up MPS-vs-CPU comparison ran out of resources. Pivoted: refactored `tools/vault_indexer/ingest/ingest_video.py` into `transcribe_whisper()` + `transcribe_canary()` branches with `--asr {whisper,canary}` flag (default `whisper`). Canary path includes ffmpeg re-encode to 16kHz mono WAV + 60s chunking with 1s overlap. End-to-end run with operator's test URL (a 23:41 Australian-accented macro podcast) via Whisper-small produced a 4,829-word transcript with numbers/percentages mostly intact; only proper-noun phonetics mangled. NeMo dep + 5GB Canary model cached on disk for opt-in usage. Tech-debt entry documents the trigger to revisit (32GB+ hardware OR confirmed real-speech success) + alternative path (CoreML variant `phequals/canary-qwen-2.5b-coreml-fp16`). New `requirements.txt` deps unchanged (NeMo not pinned — operator who opts in installs ad-hoc with `pip install "nemo_toolkit[asr]"`). Plan: [`plans/check-github-for-a-proud-glacier.md`](../../.claude/plans/check-github-for-a-proud-glacier.md). Honest finding: leaderboard claims don't always survive hardware reality; the right move was data-first, then ship pragmatically.
- **Railway cost cut — laptop-only loop gate + batched outbox drain** (2026-05-05): observed Railway bill $6.40/wk ($25.60/mo). 38% RAM, 32% CPU, 29% Postgres RAM, ~0% egress. Sync request volume confirmed irrelevant — egress was $0.0014/wk. Real cost = serverless container staying warm because **8 of 10 background loops in `app/main.py:lifespan` were running on Railway too** (accuracy_evaluator, drift_detector, digest_loop, market_data, opps_loop, macro_ingestion, hyp_tick, research_weekly, queue_worker), each self-waking on its own timer. Gated all 9 by `INSTANCE_NAME != 'railway'` (Railway gets only `outbox-purge` + tv_context expire @ daily cadence vs hourly on laptop). Removed per-analysis-job `asyncio.create_task(sync_service.drain_outbox())` from `app/analysis/service.py` and replaced with periodic 5-min drain loop on laptop side (config `SYNC_DRAIN_INTERVAL_SECONDS=300`). Outbox semantics unchanged — eventual consistency preserved, sync latency rises from instant → ≤5 min. New env vars: `TV_CTX_EXPIRE_INTERVAL_SECONDS`, `SYNC_DRAIN_INTERVAL_SECONDS`. Operator must set `INSTANCE_NAME=railway` on Railway dashboard for gate to fire. Estimated bill savings: 70-80% (Railway should drop to ~$5-8/mo). Tech-debt entry added for next lever (strip torch/Kronos from Railway image — only worth doing if `RAILWAY_FALLBACK_ENABLED=False`). 367 tests still green.
- **TV Context follow-up — Railway gate + trade-close wiring** (2026-05-04, same-day): `TVContextInbox` page now shows a laptop-only banner when `backendId === 'railway'` (sidebar nav stays visible; only the page surface is gated; backend routes still mounted on Railway so peer outbox replication via `/v1/tv-context/import` keeps working). `update_trade` in `app/trades/service.py` now fires `tv_context.service.enrich_on_trade_close` on the open → closed transition (gated by `was_closed_before` snapshot pre-update; helper itself idempotent on `tombstone.trades` dedupe). Failures logged + swallowed; trade-close response never blocked. +2 integration tests (`test_patch_trade_close_enriches_tv_context`, `test_patch_trade_close_idempotent`). Full suite 367 green. Past-decision walkthrough via `GET /v1/tv-context/by-trade/{trade_id}` is now actually fed by trade-close events.
- **TV Context layer (Phases 1-6 in one cycle)** (2026-05-04): all six phases shipped together. New module `app/tv_context/` (models, schemas, service, routes, vault, vision) — single polymorphic `tv_context_items` table with `kind` discriminator (webhook/screenshot/note/idea/event), per-category retention defaults + per-row override, hourly expire sweep that drops heavy payload + writes tombstone, webhook dedupe via SHA-256 rolling-window key. Migration 0024 adds `tv_context_items`, `hypothesis_tv_context_links`, `hypothesis.requires_tv_context` flag, `trades.context_refs` JSON column. Sync outbox extended with 4 new kinds (`tv_context_webhook|note|idea|event`); screenshots intentionally NOT replicated (vault path is laptop-only). Phase 4 gating: `/v1/research/ask` accepts `tickers` + `force_skip_context_gate` and short-circuits to `status='needs_context'` (no LLM call) when any flagged hypothesis lacks recent context for supplied tickers. Phase 5 trade-close enrichment walks `tv_context_items` in `entry_at±24h`, stamps tombstones with trade outcome, populates `trades.context_refs`. Phase 6 vision: Claude Sonnet 4.6 chart summarization on screenshot ingest, default ON, per-upload toggle, ~$0.012/image at 1024px max-width with Pillow downscale; cost logged to `payload.vision.cost_usd`, monthly tally surfaced via `/v1/tv-context/vision-spend`. Frontend: new `TVContextInbox` page at `/tv-context/:ticker?` with paste/drop screenshot modal + inline note/idea/event forms + ContextNeededBanner on Research page. ADRs [016](../decisions/016-tv-context-no-browser-automation.md) + [017](../decisions/017-tv-context-vision-default-on.md). Module doc at [tv_context.md](../modules/tv_context.md). Known gap filed in tech_debt: hypothesis daily tick can't auto-flag needs-context until `Hypothesis.tickers` becomes a first-class column.
- **Phase 3.7 — Research UI v1 (single-turn)** (2026-05-02): shipped same-day as Phase 3. ~700 lines new frontend (`frontend/src/pages/Research.tsx` + 7 components under `frontend/src/components/research/`) + 5 new react-query hooks (`useResearchAsk`, `useResearchQueries`, `useResearchQuery`, `useApproveResearchQuery`, `useDismissResearchQuery`). Backend `AskResponse` + `ResearchQueryRead` extended with `evidence` + `macro_state` + `proposed_action` so single-call render works without parsing the bundle envelope. `+1` test (`test_ask_response_includes_evidence_and_macro`), 14 research tests green. Confirm-modal Approve gates DSL mutation behind a two-step preview. **Sharp edge:** `anthropic` SDK was missing from `requirements.txt` — discovered post-deploy when first live `/ask` returned `status=error` despite key being set. Fix: add `anthropic` to requirements + reinstall + restart uvicorn. Plan: [`plans/phase-3.7-research-ui-single-turn.md`](../plans/phase-3.7-research-ui-single-turn.md). Phase 3.8 (threading) deferred — gated on operator hitting "≥3 queries / same hypothesis / 7 days" trigger.
- **Phase 3 — Research stress-test endpoint** (2026-05-02): smooth. ~1300 lines new code (`app/research/*` + `tools/vault_indexer/research_hook.py`). 13 new tests, full suite 341 green. Migration 0023 added `research_queries` audit table — applied cleanly with **no `create_all` race** (the schema-check fix from earlier in the day prevented it on first try, validating that backlog item resolution). `POST /v1/research/ask` wired end-to-end with: bundle assembler, Claude tool-use (single action: `propose_invalidator_update`), three-layer DSL validation gate, markdown answer template into `<vault>/Research/`, vault-indexer Research-tick scanner that HTTP-calls back into TradingView's approve route, weekly auto-stress per active hypothesis. ADR [015](../decisions/015-research-stress-test.md). Six parked future modes (synthesis, additional action kinds, Telegram digest, cross-hypothesis stress, multi-LLM, streaming + frontend) captured in roadmap as 8b.3–8b.8.
- **Phase 2 — Vault + indexer** (knowledge substrate, 2026-05-02): smooth. ~1100 lines new code, 9 new tests, full suite 323 green. Operator's Obsidian vault at `~/Documents/knowledge-vault/` is the source of truth; sidecar at `tools/vault_indexer/` (port 8001) over SQLite + sqlite-vec exposes `/search`, `/traverse`, `/node`, `/promote`, `/apply-renames`, `/regenerate-review`. Embeddings via `BAAI/bge-large-en-v1.5` (cached locally, cosine-native). Auto-tag via Claude Haiku constrained to `_taxonomy.md`'s 15-tag vocabulary. Operator-in-the-loop review queue is plain markdown checkboxes — no separate UI. Migration `0022_hypothesis_node_links` adds the TradingView pointer table; routes deferred to Phase 3. ADR [014](../decisions/014-vault-indexer.md). Same `create_all` race as M-2 surfaced + handled the same way; backlog item from yesterday now has its second confirmed instance.
- **Macro Workbench M-2** (hypothesis object + view registry, 2026-05-01): smooth. 23 new tests, full suite 314 green, no regressions. Sidebar widget shipped instead of full page (operator decision — defer until ≥10 active rows; 2-week revisit scheduled via cloud routine). Migration 0021 applied to laptop primary, all 6 drafts (5 thematic + btc-rally precondition chain) seeded, invalidator DSL hand-authored via `scripts/patch_invalidators.py`, first force-fire of the lifespan tick `{evaluated: 6, expired: 0, invalidated: 0}` — all 6 active on 2026-05-01. ADR [013](../decisions/013-hypothesis-object.md) captures the 10 locked decisions. Boot-sequence wart noted: `Base.metadata.create_all` raced ahead of `alembic upgrade head` and required hand-cleanup; backlog entry filed for the long-term cleanup.
- **Macro Workbench M-1** (signal layer, 2026-04-30): smooth. 38 symbols cached, `/v1/macro/{series,ratio,refresh}` live, daily ingestion loop running.
- **Phase 0** (snapshot): smooth. Tag + DB dump + bundle archive + ROLLBACK.md. Operator-driven Railway dump deferred (used Railway's built-in backup feature).
- **Phase 1.1** (accuracy backfill): smooth. 15 new tests covered math + integration; idempotency via `UNIQUE(prediction_id)` worked first try.
- **Phase 1.2** (`/accuracy` UI): smooth. Heatmap + drilldown built before live data — proved the "build-now-iterate-on-data" approach.
- **Phase 1.3** (drift + Telegram): backend complete; Telegram dormant pending operator bot setup ([backlog.md](backlog.md) Unlock #1).
- **Phase 2.1** (empty states): trivial as expected.
- **Phase 2.2** (lightweight-charts v5): **DEFERRED** — not on critical path; tracked separately.
- **Phase 3.1+3.2** (opportunities): smooth. Hardcoded rules + UNIQUE(prediction, rule) idempotency. Frontend tabs + cancel modal landed cleanly.
- **Phase 4** (Telegram digest): wired into Phase 1.3 commit. Daily loop ready; dormant pending setup.
- **Phase 5** (trades): smooth. P&L attribution by-rule will only be meaningful after several real trades.
- **Phase 6** (IV runway): yfinance ATM IV + earnings, daily refresh. Silently collecting.
- **Post-roadmap (queue + Neumorphism)**: shipped after Phase 6 — see [queue.md](../modules/queue.md), neumorphic redesign in [decisions/004](../decisions), [decisions/005](../decisions).
- **2026-04-30 — Accuracy + By Horizon visual update**: composite Accuracy metric (`hit% / MAPE%`) replaces hit-rate-only color so directionally-lucky-but-magnitude-wrong models stop reading green; added `1d | 1h | all` interval toggle (backend `interval` filter on `/v1/accuracy/grid`, no migration — field already on rows); low-n masking (`n < 4` greyed); click-drill replaced with hover/focus inline panel. By Horizon cells: `$` prefix removed from Δ% values, repeated "forecast" sub-text moved to legend, secondary reference now actual close (or italic predicted close for forecast-only cells), hover tooltip with side-by-side OHLC mini-candles. See [accuracy.md](../modules/accuracy.md) and [predictions.md](../modules/predictions.md) for cell semantics.
- **2026-04-30 — Self-healing OHLCV fetch**: accuracy evaluator's hourly tick now refreshes upstream OHLCV when a pending prediction's actual is missing; deduped per (ticker, interval) per tick, capped via new `ohlcv_fetch_misses` table after 24 attempts so unfetchable bars stop hammering yfinance. Sibling lazy-refresh added in `_process_task` warms caches for cold-start intervals (e.g. first 1h schedule). Resolves [backlog.md](backlog.md) Unlock #2. See [decisions/010](../decisions/010-self-healing-ohlcv-fetch.md).
- **2026-04-30 — Scheduler mid-tick PUT race**: `record_run` now recomputes `next_run_at` against the freshly-loaded config (instead of a snapshot taken at tick start), so a `PUT /v1/schedule` landing during execution is honored on the way out — no more lost slots. No new flag, no new column. See [decisions/011](../decisions/011-schedule-mid-tick-put-race.md).
- **2026-04-30 — Docs hub + jobs list redesign**: new `/docs` route (lazy-loaded). Markdown-driven reference docs with sticky scroll-spy TOC, segmented document switcher, persistent font-size control. v1 ships `metrics.md` (terms, formulas, color legends across the platform); `how-to-use.md` stubbed. Adding a doc = drop `.md` in `frontend/src/docs/` + register slug. `/analysis` page rebuilt: expandable rows replace the JobID-first table. New columns surface a friendly run summary (sym count · intervals · model), a stacked outcome bar (done/running/ineligible/error counts, lazy-fetched per row), smart relative timestamps (Today / Yesterday / MMM D), and duration. Per-task breakdown opens inline on row click — no round trip to the detail page for everyday triage. New deps: `react-markdown`, `remark-gfm`, `rehype-slug`. See [frontend/pages.md](../frontend/pages.md).
- **2026-04-30 — Macro Workbench M-1 (signal layer)**: foundation for the regime-aware research workbench. New `macro_series` table (`migrations/0019`) holds daily close-only values from yfinance (~30 symbols) + FRED (~6 economic series). New module `app/macro/` with provider abstraction, hand-curated `registry.yaml`, service (`refresh`, `refresh_all`, `get_series`, `compute_ratio`), three `/v1/macro/*` endpoints, and a daily lifespan ingestion loop. Ratios are computed on demand (not materialised). Chunked-upsert (1000 rows/batch) avoids Postgres' 32767 bind-param limit on long-history symbols. 16 new tests (idempotency, value updates, ratio inner-join, zero-denom skip, registry fan-out, error isolation, route round-trips, FRED CSV parser, chunked-upsert regression). First live tick: 38/38 symbols ok, 220k rows. See [macro.md](../modules/macro.md), [decisions/012](../decisions/012-macro-workbench-storage-shape.md), [plans/M-1-signal-layer.md](../plans/M-1-signal-layer.md).
- **2026-04-30 — Macro Workbench M-1f (frontend)**: new `/macro/:tab?` route, lazy-loaded. Three sub-tabs — Overview (4 regime panels × 3 sparkline rows, inline-expand to focused chart), Ratios (full-size focused chart with quick-switch dropdown across the 12 canonical ratios), Sectors (9-cell sector-vs-SPY strip with Δ%-coloured cells, click-to-expand). Default window 5Y; chips `1Y/3Y/5Y/10Y/Max`. Sparklines hand-rolled SVG at weekly close; focused charts use `lightweight-charts ^4.2.1` themed for the neumorphic palette (no new deps). Single source of truth for which ratios live where: `frontend/src/lib/macro-views.ts`. Sidebar entry between Trades and Docs. UI patterns informed by the `ui-ux-pro-max` skill (Data-Dense + line + grouped-bar) + `frontend-design`; neumorphic theme preserved — no dark mode, no glassmorphism. See [macro.md § Frontend](../modules/macro.md#frontend-macro-lazy-loaded) and [plans/M-1f-frontend.md](../plans/M-1f-frontend.md).
- **2026-05-01 — Multi-watchlist + lightweight quotes + sector drill-in (MW-1 / MW-2 / MW-3)**: split the dual role of the original watchlist concept. **MW-1**: rebrand UI `/watchlist` → `/roster` (operational; drives Kronos). Backend untouched. **MW-2**: new `/v1/boards` module + `/watchlists` page for casual ticker lists. New `boards` + `board_tickers` tables; `ticker_market_data` gains `last_close`, `last_close_at`, `pct_1w`, `quote_fetched_at` (migration 0020). Existing daily market-data cron extended to walk roster ∪ boards (one new ~15-day yfinance call per ticker). Move-to-other-list, link-out to TradingView (no in-app charts for casual tickers — operator's brokerage covers that). 15 new tests. **MW-3**: hardcoded top-10 holdings per sector ETF (`frontend/src/lib/sector-holdings.ts`) + `/v1/quotes` bulk endpoint. When a sector cell is expanded on `/macro/sectors`, a "Top holdings" panel renders below the chart with last close + 1w Δ% per holding. 289 backend tests passing. See [boards.md](../modules/boards.md), [plans/multi-watchlist-and-quotes.md](../plans/multi-watchlist-and-quotes.md).
- **2026-05-01 — Macro Workbench M-1.5 (stagflation regime panel)**: 5th macro panel "Inflation regime" added for tracking stagflation/inflation cycles. New symbols ingested: yfinance `^VIX` and `DBC` (broad commodities); FRED `DFII10` (10Y real yield), `T5YIE` (5Y breakevens), `PPIACO`, `CPIAUCSL`, `MORTGAGE30US`. New backend `/v1/macro/spread` endpoint with weekday-aligned forward-fill (default 7-day window) so weekly FRED series publishing on different weekdays — like `MORTGAGE30US` Thursdays vs `WGS10YR` Fridays — line up. Adds VIX to the Stress panel and Mortgage spread to Liquidity. Frontend: `RegimePanel` row type expanded to support spread rows; per-row `<InfoBubble>` (i) icons drive from `lib/glossary.ts` (~21 new entries). Tooltip width bumped to 320px and `whitespace-normal break-words` for the wordier definitions. Dashboard `RegimeStrip` now shows 5 tiles. New hypothesis draft `stagflation-regime-24m.md` added — 12 tracking signals across the new panel + cross-axis confirmers, four invalidator categories. 273 backend tests passing (3 new for spread endpoint + alignment regression). See [macro.md § Frontend](../modules/macro.md#frontend-macro-lazy-loaded) and [hypotheses/draft/stagflation-regime-24m.md](../hypotheses/draft/stagflation-regime-24m.md).
- **2026-04-30 — UI consolidation (Phases A → E)**: cross-product polish pass. (A) Sidebar IA — 11 flat entries collapsed into 4 logical groups (Dashboard / Decisions{Macro/Predictions/Motion} / Admin{Watchlist/Schedule/Health} / Docs); new `Predictions` and `Motion` tab wrappers; `/analysis` rebranded to `/health`; legacy URLs (`/by-horizon`, `/opportunities`, `/trades`, `/analysis/...`) keep working via `<Navigate replace />`. (B) Dashboard rebuilt around regime context — 4-tile RegimeStrip (1Y Δ% + sparkline per axis), Latest Opportunity card, Accuracy 30d snapshot tile, slimmed Schedule + Queue + Recent jobs. (C) Density pass — new `PageHeader`, `EmptyState`, `LoadingStates` primitives in `components/common/`; five high-traffic pages migrated. (D) Tooltip standard — single `HoverTooltip` primitive + new `InfoBubble` (i)-circle component reading from `lib/glossary.ts` (~25 entries) so any data label can carry a hover-able inline definition with a "Read more" link to `/docs/metrics`. (E) Mobile pass — added `overflow-x-auto` on AnalysisJobs/Watchlist/TickerLabels/PredictionsByTarget table containers. UI patterns informed by `ui-ux-pro-max` (data-dense + grouped-bar) and `frontend-design`. Five commits, one per phase, with TS clean + 270 backend tests green between each. See [plans/UI-consolidation.md](../plans/UI-consolidation.md).

## North-star principles

1. **Trust before action.** Every "tradeable" claim must be backed by visible accuracy data sliced by `(ticker, horizon, model)`. Aggregate stats lie.
2. **Cheap reversibility.** Every phase tagged in git; rollback path documented in `backups/ROLLBACK.md`.
3. **Single-user, opinionated UX.** No auth tiers, no permissions. The cost of premature generality is higher than the cost of refactoring later.
4. **External channels stay external.** This app is the *quantitative pillar*; news, policy, expert commentary, manual TV chart reads stay out of scope. Plug in only data the model itself emits or that's required to evaluate it.

## Locked decisions (2026-04-27)

| Decision | Choice |
|---|---|
| Accuracy metrics | MAPE, RMSE, directional hit-rate (all three, computed per `(ticker, horizon, model)`) |
| Backfill scope | Existing prediction history only — no special historical batch job |
| Drift threshold | Recent-30d MAPE > 1.5× all-time MAPE for that pair triggers flag (start strict, tune after 1 week of data) |
| Notification channel | Telegram bot (single channel for v1; email deferred) |
| Sequencing | Snapshot → Trust → UX → Actionability → Push → Journal → Options runway |

---

## Phase 0 — Snapshot (rollback safety) · ~30 min

Capture the current cloud-deployed v1 (CF Pages frontend at `tradingv-83b.pages.dev` + Railway backend) in case Phase 1+ goes sideways.

Artifacts (in `backups/` — gitignored except docs):
1. **Git tag** `v1.0-pre-trust-sprint` annotated at current main HEAD.
2. **Laptop Postgres dump** via `pg_dump` of `.env.laptop`'s `DATABASE_URL`.
3. **Railway Postgres dump** — operator-driven (Railway CLI or psql via Railway DATABASE_URL).
4. **OpenAPI JSON snapshot** from Railway (`/openapi.json`) — captures API contract.
5. **Frontend bundle archive** — copy `frontend/dist/` to `backups/frontend-dist-YYYY-MM-DD/`.
6. **Env-var inventory** — operator-driven copy of Railway + CF Pages env vars to `backups/env-inventory-YYYY-MM-DD.md` (gitignored, contains keys).

Rollback path: `backups/ROLLBACK.md` (committed) names exact commands to revert: git checkout the tag, restore SQL dumps, rollback CF deployment to specific hash, re-set env vars.

**Note**: Laptop DB and Railway DB are bidirectionally synced via Tailscale (Phase B1+B2). A laptop dump is functionally a Railway dump modulo unsynced rows in flight. Both are still captured for paranoia.

---

## Phase 1 — Trust through feedback · ~1.5 weeks · PRIORITY

**One mandate**: prove or disprove Kronos accuracy at every `(ticker, horizon, model)` pair you might trade.

### 1.1 — Accuracy backfill + persistence (~3 days)

New table `prediction_accuracy`:
```sql
prediction_accuracy(
  id, prediction_id FK, ticker, horizon_bars, model_id,
  predicted_close NUMERIC, actual_close NUMERIC,
  error_pct NUMERIC,           -- (actual - predicted) / actual
  error_abs NUMERIC,           -- |actual - predicted|
  direction_correct BOOL,      -- sign(predicted_change) == sign(actual_change)
  generated_at TIMESTAMPTZ,
  evaluated_at TIMESTAMPTZ,
  UNIQUE(prediction_id)        -- idempotency
)
```

Background job `accuracy_evaluator()` ticks daily after market close: pulls all `prediction_points` whose horizon has elapsed and aren't yet in `prediction_accuracy`, fetches `actual_close` via existing OHLCV provider, computes errors, inserts row. Idempotent.

Backfill: run once on existing `prediction_points` history (~5 jobs of data). No special historical batch.

### 1.2 — Per-(ticker, horizon) accuracy dashboard (~3 days)

New page `/accuracy`:
- **Heatmap table**: rows = watchlist tickers, columns = horizons in scheduled run (1d, 3d, 5d, 10d). Cells = directional hit-rate over last 30 evaluated predictions (color-graded: green ≥ 60%, yellow 50-60%, red < 50%). Secondary metric (MAPE %) shown on cell hover.
- **Drilldown** on cell click: scatter plot predicted vs actual (diagonal = perfect), MAPE/RMSE/hit-rate trio, sparkline of accuracy over time, list of recent prediction ↔ actual pairs.
- **Filters**: date range, model_id (when ensemble lands).

### 1.3 — Drift detection + Telegram alerts (~3 days)

Daily cron compares last-30d MAPE to all-time MAPE per `(ticker, horizon, model)`. Flag if recent > 1.5× all-time AND ≥ 10 evaluations in recent window (avoid noise on sparse data).

New table `drift_alerts(id, ticker, horizon, model_id, recent_mape, allTime_mape, ratio, flagged_at, acknowledged_at)`.

Telegram integration:
- Bot setup via @BotFather (one-time, manual).
- New env var `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID` (your DM with the bot).
- New `app/notifications/telegram.py` posts markdown messages.
- Drift alerts → Telegram immediately. Daily morning digest (Phase 4) reuses same channel.

Drift alerts also surface as a banner on `/accuracy` and `/` (dashboard) until acknowledged.

### Phase 1 exit criteria (gates Phase 3)

- ≥ 1 `(ticker, horizon)` pair with directional hit-rate ≥ 60% over ≥ 20 evaluations.
- Accuracy dashboard renders without empty states for all watchlist tickers.
- Telegram drift alert verified end-to-end (force a drift via test data).

If exit fails → pivot to retraining / model-side investigation, not Phase 3.

---

## Phase 2 — UX hardening · ~3 days · parallel-able with Phase 1

### 2.1 — Empty states pass

By-target, by-horizon, analysis pages currently render blank when there's no data. Replace with a meaningful "No data yet" UI per page, with a single CTA pointing at the first-action ("Run your first analysis" → opens Run dialog).

### 2.2 — lightweight-charts v4 → v5 upgrade

v5 brings: crosshair sync between panes, drawing tools, multi-pane (sets up prediction-vs-actual overlay for Phase 1.2), better mobile touch. Breaking changes minor; check release notes.

Bonus: add prediction-vs-actual line overlay on `/predictions/by-target` chart — doubles as a Phase 1 visualization.

(Plotly explicitly NOT chosen here — see backlog entry "Charting library — lightweight-charts v5 chosen over Plotly".)

---

## Phase 3 — Actionability bridge (signals layer) · ~2 weeks

**Predicate**: Phase 1 exit criteria met.

### 3.1 — Opportunities table + signal generator

```sql
opportunities(
  id, ticker, kind ENUM('buy','sell'), generated_at,
  source_prediction_id FK, source_model_id,
  rule_id, rule_label,
  predicted_move_pct NUMERIC, confidence NUMERIC,
  status ENUM('open','acted','expired','dismissed'),
  expires_at,
  acted_at, dismissed_at, dismissed_reason
)
```

Hardcoded rules to start (NOT a DSL):
- `R1`: predicted_move_pct ≥ +2% over 5d AND historical_hit_rate ≥ 0.60 → BUY.
- `R2`: predicted_move_pct ≤ -2% over 5d AND historical_hit_rate ≥ 0.60 → SELL.
- `R3`: predicted_move_pct ≥ +5% over 10d AND historical_hit_rate ≥ 0.55 → BUY.
- (Tune after 2 weeks of real signal data.)

Generator runs after each scheduled prediction batch.

### 3.2 — Opportunities UI

New page `/opportunities`:
- Today's open list (sorted by confidence × predicted_move).
- History tab (acted + dismissed + expired with realized outcomes).
- Per-row actions: mark acted, dismiss (with reason).

---

## Phase 4 — Daily Telegram digest · ~2 days

Predicate: Phase 3 generates ≥ 1 opportunity per typical day.

Cron at 8 AM operator-tz posts to Telegram:
- Top N open opportunities (markdown table).
- Any unacknowledged drift alerts.
- One-line summary of last night's run.

Reuses Telegram infra from Phase 1.3.

---

## Phase 5 — Trade journal · ~1 week

Predicate: 2 weeks of opportunities feed observed; user has acted on ≥ a few.

```sql
trades(
  id, opportunity_id FK NULLABLE, ticker, side ENUM('buy','sell'),
  qty INT, entry_price NUMERIC, entry_at TIMESTAMPTZ,
  exit_price NUMERIC, exit_at TIMESTAMPTZ,
  realized_pnl NUMERIC, fees NUMERIC, notes_md TEXT
)
```

Manual entry. Brokerage-API integration explicitly out of scope (single user, friction not worth it). One-click "create trade from opportunity" button on opportunities page prefills fields.

`/trades` page: list view + entry form + simple P&L summary (today / week / month / all-time).

Per-opportunity P&L attribution: "If you'd taken every BUY opportunity Kronos surfaced from rule R1, your P&L would be X." Closes the feedback loop on whether Kronos is worth trading on.

---

## Phase 6 — Options runway data layer · ~2 days · tucks into Phase 3 sprint

Single background job: pull IV percentile + earnings date for each watchlist ticker (free source: yfinance, polygon free tier, or similar). Two new columns on `tickers` (or new `ticker_market_data` table — TBD). No UI yet. Just data accumulating.

Sets up the eventual options chapter (strategy generator, IV surfaces) without blocking Phase 1-5.

---

## Out of scope (explicit)

- News/policy/commentary ingestion (user owns these channels manually)
- Brokerage API integration (single user, manual entry friction acceptable)
- Multi-user / auth tiers / sharing
- Mobile-first responsive layout (Telegram serves mobile; desktop-first web is fine)
- Backtesting infrastructure beyond what `prediction_accuracy` provides
- Multi-model ensemble (defer until at least one second model exists)
- Public tunnel for laptop (separate backlog)

---

## Estimated total

~5 weeks of focused work to "decision-support tool", gated phase-by-phase. Stop early at any phase whose exit criteria don't hit — that's the signal that Kronos isn't ready for the next layer of investment.
