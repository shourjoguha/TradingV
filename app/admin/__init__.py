"""Admin module — loop registry, app_settings cascade, process_status.

Phase 4 of the cost-aware iteration plan. Surfaces every laptop-side
background loop in the new ``/admin/processes`` + ``/admin/cadences`` UI
with status, manual fire, abort, and cadence editing. Also houses the
Anthropic kill-switch + monthly cap that gate Research + TV vision.
"""
