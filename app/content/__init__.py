"""content — video-series hierarchy (Domain -> Series -> Arc -> Episode).

Build-in-public series feature. Models the editorial tree and binds each
episode to a real source artifact (`source_ref`) so the verifiability
discipline from the demo branch extends to video.

Design + rationale: `.claude/plans/video-series-platform-design.md`.
"""
