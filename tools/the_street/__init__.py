"""The Street — smart-money snapshot helpers.

Read-side CLI and library for querying smart-money snapshots stored under
``<vault>/The Street/snapshots/``. Periodic-update pipeline is out of scope
for v1; see ``<vault>/The Street/_README.md``.
"""
