# Backups directory

Local-only snapshots for rollback safety. Tracked files:
- This README
- ROLLBACK.md (rollback procedure)

Gitignored: SQL dumps, env-var inventories, frontend-dist archives. Too large or contains secrets.

