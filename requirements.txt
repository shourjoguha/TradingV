fastapi
uvicorn
sqlalchemy>=2.0
asyncpg
psycopg2-binary
pydantic>=2
pydantic-settings
greenlet
alembic
httpx
anthropic
yfinance
pandas
pytest
pytest-asyncio
aiosqlite
pyyaml
Pillow
python-multipart

# Vault auto-ingest (Phase: video-channel-auto-ingest).
yt-dlp
feedparser
openai-whisper

# Video-vision L2 (Scene + OCR). Requires `brew install tesseract` system
# binary too. See .claude/modules/video_vision.md.
pytesseract

# MLX acceleration for ASR + VLM on Apple Silicon (M1/M2/M3/M4). On Linux,
# pip install of these may fail; the adapters (`whisper_adapter.py` and
# `vlm_adapter.py`) detect Apple Silicon and fall back to torch / no-op
# gracefully when MLX is unavailable.
#   - mlx-whisper: 3-5× faster transcription on M3 vs openai-whisper CPU
#   - mlx-vlm: Qwen2-VL-2B for L3 semantic captions (~1GB model on first run)
mlx-whisper
mlx-vlm

# Kronos runtime (always installed; KRONOS_ENABLED toggles use, not install).
torch>=2.0.0
huggingface_hub==0.33.1
einops==0.8.1
safetensors==0.6.2
